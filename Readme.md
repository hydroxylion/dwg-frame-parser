# dwg-frame-parser（DWG/DXF 图框解析工具）

> 技术栈：Flask + ezdxf + 原生 JS（前端）

上传 DXF/DWG 图纸，后端智能识别图框并返回宽×高（mm）、图框数量及候选明细。

---

## 〇、当前状态（2026-09-17 交付）

| 项目 | 状态 |
| --- | --- |
| 回归基准 | 31 张校验图纸（含目标图），本轮改动后**一致 30 / 变化 1**（唯一变化 = 目标地下室电力t3.dwg：84100×14 → 105100×11 + 84100×6，fc 32 不变） |
| 后端测试 | `test_backend_rules.py` **51 通过 / 0 失败**（+8：附栏外扩纯函数 6 + 端到端 2） |
| 前端测试 | 7 个用例文件合计 **222 项全绿**（+11：名称双行结构化用例组） |
| 本轮交付 | **图框口径切换：主图框+图签附栏整体输出（105100口径）**——地下室电力t3.dwg 检出尺寸组变为 105100×59400×11，与用户实测完全一致 |

**算法环境变量开关**（仅用于回归 A/B 对照，生产不设置）：

| 开关 | 作用 |
| --- | --- |
| `FRAME_PARSER_NO_DENOM_GUARD=1` | 关闭 rel 分母塌缩防护 |
| `FRAME_PARSER_NO_GRID_RESCUE=1` | 关闭布局空间网格图框救援 |
| `FRAME_PARSER_NO_LAYOUT_WINS=1` | 关闭布局空间优先清洗 |
| `FRAME_PARSER_NO_COND_G=1` | 关闭同模板缩放套图救援（条件G） |
| `FRAME_PARSER_NO_COND_H=1` | 关闭主导比例内容救援（条件H） |
| `FRAME_PARSER_NO_COND_I=1` | 关闭表格内容页救援（条件I） |
| `FRAME_PARSER_NO_SOLO_RECHECK=1` | 关闭孤证复核（无互证的纯相对面积候选剔除） |
| `FRAME_PARSER_NO_WRAP_DOM=1` | 关闭包裹框主导比例剔除 |
| `FRAME_PARSER_NO_TITLE_STRIP=1` | 关闭图签附栏外扩合并（105100口径） |

**日志工具开关**：

| 开关 | 作用 |
| --- | --- |
| `FRAME_PARSER_LOG_LEVEL=DEBUG` | 排查模式：逐候选明细 + ezdxf 内部告警写入 parser.log 并刷控制台 |

---

## 一、图框识别算法（核心流程）

### 1. 候选收集（模型空间 + 所有布局空间）

| 候选来源 | 说明 |
| --- | --- |
| 闭合多段线 | LWPOLYLINE/POLYLINE，含矩形度过滤（≥0.92，剔除 L 形标题栏等） |
| INSERT 块参照 | 块定义 bbox × xscale/yscale + rotation + insert 点 = 真实图框 bbox；预筛短边 ≥500mm（**或命中标准 A 系列短边 210/297/420…**，A4 等小块图框块可入库；**或命中"布局空间网格排版图框"救援**，见下）且长宽比 ∈[1.05, 5.5] |
| 直线矩形 | 共线聚合 + 线对配对 + P1 严格矩形判定（4 边各有完整 LINE 覆盖）；密集聚类时**长边（跨度≥180）全配对**找回远距离图框边界，线对按公共覆盖长度排序；组合扫描按**竖对 x 起点落在横线公共覆盖段内（xa∈[xLo,xHi] 且 xb≤xHi）、xa 升序**遍历——帧左边界对排最前命中即确认，坐标直接排除大量无效竖对，杜绝高密度图（数万线对）预算耗尽漏检 |
| 全实体包围盒（降级） | 以上全无时兜底；**排除 VIEWPORT 与文字/标注类实体**（TEXT/MTEXT/DIMENSION 等——文字允许外溢，不撑宽图框） |

**坐标处理**：
- 实体坐标统一 **OCS→WCS** 转换（`get_polyline_vertices`/LINE 端点经 `ocs().to_wcs()`），兼容带倾斜 extrusion 的实体
- **全局旋转矫正**：自定义 UCS 整体倾斜的图纸（实体坐标斜存 WCS），按长度加权直方图估计主方向，主体共旋占比 ≥65% 才施加旋转矩阵"转正"（防混合方向图纸误转），使矩形度 / 水平垂直检测恢复有效

**可见性过滤**（收集前）：跳过冻结/隐藏图层实体 + 实体级 invisible（组码 60），只统计 CAD 里可见的实体。

**布局空间「网格排版图框」救援**（INSERT 预筛的补充通道，2026-09-11 修法A）：仅对**图纸空间布局**生效（模型空间不做救援）。判据 = 同块名同尺寸（宽高各 ±2mm）INSERT **≥4 个** + 长宽比 ∈ 图框区间 + **排列成网格**（按 X/Y 一维聚类得列数 ≥2、行数 ≥2，网格覆盖率 ≥80%）。命中即豁免这些实例的短边下限，使其进入候选库（是否算图框仍由 `is_frame_like` 决定）。动机：布局空间本就是排图纸页的地方，出现多个等大框是"一版多页"的强信号，与绝对尺寸无关。典型：DS4 四层阁楼平面系统图 —— 6 个 A4 页面框按 1.0475 倍画在布局空间（434.1×311.1，短边 311.1 <500 且不在 A 系列），原本被预筛整体剔除。开关：`FRAME_PARSER_NO_GRID_RESCUE=1`。

### 2. 图框特征匹配（is_frame_like，按序判定）

- **前置**：归一化长宽比 ∈ [1.05, 5.5]；`area_ratio > 1.0 + 0.1%`（候选面积超过 layout 总面积，含浮点容差）直接判异常剔除
- **条件 A**：面积占比 ≥ 15%（常规单图框 layout）
- **条件 C**：相对面积 ≥ 该 layout 最大候选面积 × 10% **且长宽比 ≤ 2.5**（上限剔除细长内容块 bbox 冒充图框）；长宽比下限按类型分流——**直线矩形放宽到 1.10**（P1 严格矩形，四边各有完整 LINE 覆盖，是刻意画的框，几乎无"内容轮廓冒充"风险；非等比拉伸的大页面框 ratio 1.17 也放行），**闭合多段线 / INSERT 块 / 降级包围盒维持 √2×0.95≈1.343**（近方形内容外框 1.06/1.21/1.24 仍被挡）。**rel 分母排除内容级底图大框（两条判据互补）**：① 面积占比 ≥50% 且长宽比偏离 √2 超 10%（排版大外框包住全部页面会稀释真页框 rel）；② 面积占比 ≥15% 且长宽比 >2.5（超宽内容条带/排版外框，底框外大量内容拉大 layout 总面积时逃过 ①）。这类大框最终由包裹剔除收掉，真整页图框即使占 100% 也近 √2 且 ratio ≤2.5，两条判据都不命中，不受影响
- **条件 B**：显式矩形 + 短边 ∈[400, 2000]mm + 宽高接近整数 + 长宽比接近 √2±10% **+ 相对面积 ≥10%**（密集几何场景，过滤家具/设备小矩形与 CAD 远处/主框区外的孤立小矩形；直线矩形与闭合多段线统一要求）
- **rel 分母塌缩防护**（在 rel 计算前统一判定）：若某 layout 的 rel 分母（最大候选面积）**占该 layout 总面积 < 1%**，**且**存在**占 layout 面积 ≥50% 的被排除大框**，则判定为**「整图即图框、内无图框级参照系」**（典型：1:1 设备布置图/总图，整图外轮廓因长宽比偏离 √2 被排除，分母塌缩到图内某个设备构件，导致所有构件 rel 虚高数十倍而误入条件 C）——该空间所有候选的 `rel_area_ratio` 一律置 **0**，只保留条件 A（占比 ≥15%）与条件 B 通道。开关：环境变量 `FRAME_PARSER_NO_DENOM_GUARD=1` 可关闭（仅回归对照用）
- **条件 D**：同 layout **同尺寸直线矩形 ≥2 份** + 长宽比接近 √2±10% + 短边 ≥500 + 相对面积 ≥1%（多尺度"套图"里小页框面积远小于最大图框仍可识别；rel≥1% 排除重复表格/阵列矩形）
- **条件 E（块模板标准幅面 / A+ 通道）**：**块参照**的块定义本身是标准图幅（短边 ∈ {841,594,420,297,210,148,105} 且长边 ≈ 短边×√2，容差 1mm）+ 实例尺寸 / 块定义尺寸 = k 为**等比**（宽高比值一致，兼容 rotation 90° 宽高互换）+ k 落在 `COMMON_PLOT_SCALES` 常用制图比例白名单（1/2/2.5/4/5/10/20/25/40/50/75/80/100/150/200/250/300/400/500）→ 放行。判据**不依赖面积占比与绝对尺寸**，只看"模板是标准图幅 + 倍率合法"，解决同一标准图幅块被以多种比例重复插入（尺寸跨度大）时的漏检；家具/门窗符号块定义不在 A 系列、自定义倍率（39.05× 等）被白名单拦下
- **同块名族"一荣俱荣"传播**：按块名分组，族内只要有 ≥1 个实例通过既有判定（A/B/C/D/E）→ 整族一并放行；约束**族内所有实例长宽比均接近 √2（±10%）**，任一实例比例异常即整族放弃传播（装饰块阵列比例远离 √2，不会被连带放行）。覆盖自定义尺寸图框块（块定义非 A 系列，但族内大尺寸实例已靠条件 A/C 通过）——"这个块是图框"是整体事实，同一块画了 N 次没有理由只有最大的那张算图框
- **条件 H（主导比例内容救援，2026-09-14）**：未入选的**闭合多段线/直线矩形**候选若同时满足 ① 长宽比与同 layout 内 **≥3 个已入选图框共享的主导比例一致（聚类与匹配均取 ±0.5%）**——同模板缩放出图比例是指纹，模板拷贝精确到 1e-6 量级，0.5% 已足够宽松；取 0.5% 而非 1% 是防"近邻比例混簇"（新古典式 21728×15365 ratio 1.4141 与其内框 21302×14939 ratio 1.4259 相差 0.83%，±1% 下会合并成伪主导簇导致家具详图边框 3900×2720 被误救；±0.5% 下两簇各自凑不满 3 → 该图 H 自动失效）② bbox 内**完全包含 ≥12 个实体**（真页面框内必有大量图纸内容，实测 52~839；比例恰好落在主导比例附近的窗户 decoy 1277×882 ratio 1.4478 框内只有自身边线，实测 5~6 个，被内容数干净排除）③ 已被某个"已入选"图框完全包住的候选不救（嵌套去重反正会剔，救入只是噪音——1280×1850 虚线窗套实证）→ 救援放行。动机：春风公寓２ 的 21 个图框是同模板四种比例排版（ratio 全部 = 1.4444），右侧 5 张小图框（17472×12096 / 14560×10080×3 / 10735×7432）标题栏只有一条全高分隔线、没有横向分隔线——条件 G 的条纹签名不命中，其余通道也全堵（rel 3.7~9.9% / area_ratio 0.1~0.4% / 短边 >2000 / 非直线矩形）。与条件 G 的分工：G 靠"标题栏条纹"结构签名，H 靠"比例指纹 + 框内内容量"，两签名互补覆盖标题栏画法不同的套图。开关：`FRAME_PARSER_NO_COND_H=1`
- **包裹框主导比例剔除（2026-09-14）**：包裹框剔除补第三判据——某内部尺寸组的比例**命中主导模板比例（±0.5%，与条件 H 同口径）**且与外框自身比例差异 **>10%**（排除同框重复画法，重复画法比例必与外框一致）→ 外框是"内容区边界"而非图框，即使内部只有 1 个尺寸组也剔除。动机：春风公寓２ 的 `67627×31514`（ratio 2.146，虚线层）内容区边界包住 1 张 `26000×18000` 主导页框，旧规则"组数 ≥2 或 同组 ≥3"都不满足 → 漏剔 → 去重"留大剔小"把真页框吃了。开关：`FRAME_PARSER_NO_WRAP_DOM=1`
- **条件 G（同模板缩放套图救援，2026-09-14）**：**直线矩形**候选带「标题栏条纹」结构签名（框右侧/左侧 25% 区域内两条覆盖 ≥80% 框高的竖线——框边界本身也算候选边——围出带宽 ∈[5%,25%]×框宽 的竖带、带内横向分隔线 ≥2 条）**且**长宽比与同 layout 内某个**已入选直线矩形图框一致（±1%）**→ 二扫救援放行。动机：同一模板按多种出图比例（1×/1:2.5/1:4/1:4.46 非公度倍率）排版的"套图"，小尺寸实例 rel 6.25%/5.02% 过不了条件 C、短边 4613/4133 超条件 B 上限、尺寸互不相同条件 D 也不适用——全通道否决漏检；但"同模板"意味着长宽比精确不变，标题栏条纹是所有实例共享的结构特征（同图 10 个房间/构件矩形 decoy 全部不命中，柜/架类符号条纹带宽仅 ≈2.4%×框宽被 5% 下限滤除）。条纹签名在候选收集阶段预计算（仅图框级 ratio 区间、面积前 400 的直线矩形，性能有护栏）。典型：胜利公寓２（3×26305×18450 + 10522×7380 + 6576×4613 + 5892×4133，六框 ratio 全部 = 1.4257）。开关：`FRAME_PARSER_NO_COND_G=1`

### 3. 候选清洗（匹配通过后）

1. **包裹框剔除**：内部完全包含 ≥2 个独立尺寸图框候选的大框判为外包络剔除（按 5% 尺寸聚类去重，避免同图框重复画法误剔；单尺寸组实例 ≥3 也判为拼版外框；跳过 INSERT 块参照；**自身长宽比接近 √2±10% 的显式矩形豁免**——纸张边框不是拼版外框，内部内容交由嵌套去重收掉）
2. **对齐排列剔除**：同块名 ≥3 个 INSERT 实例短方向紧贴成线判为装饰块（柱网/门窗阵列）；**白名单保护**（不参与剔除）：标准 A 系列整数尺寸（短边 ∈{841,594,420,297,210,148,105} 且长边 ≈ 短边×√2）**或长宽比接近 √2±10%** 的块——图框块（含自定义尺寸模板）多张并排时不误剔，装饰块比例远离 √2 不受影响
3. **去重**：嵌套（面积 < 外框 95%）+ IoU > 50%
4. **模型空间降级框清洗**：布局空间已识别真图框时，剔除模型空间的"全实体包围盒"（模型空间仅是内容区）
5. **布局空间优先**（2026-09-11 修法A）：若**布局空间**（非模型空间）已识别出 **≥4 个**图框且模型空间也有候选 → 剔除**模型空间的全部候选**。理由：布局空间是出图页面空间，一旦其中排出多个等大页面框，模型空间就只是这些页面的内容源，其内的任何大框都不是图框。与第 4 条同源，但更彻底（那条只清"全实体包围盒"）。开关：`FRAME_PARSER_NO_LAYOUT_WINS=1`
6. **每 layout 上限 100**（防爆炸兜底）

### 4. XREF 外部参照警告

ezdxf 默认不加载 XREF 外部文件，引用块会漏识别图框。检测到时写日志警告（不阻塞解析），API 返回 `xref_warnings` 字段供前端展示。

### 5. 输出

- `width` / `height`：选中图框尺寸（面积最大候选，可按优先级加权）
- `frame_count`：图框数量
- `frame_counts_by_layout`：按布局分组的图框数（如 `{"模型空间": 3, "布局 \"Sheet1\"": 2}`）
- `candidates`：候选明细（前 20）
- `xref_warnings`：XREF 警告列表

---

## 二、测试基准（回归场景）

| 场景 | 实际图框数 | 验证要点 |
| --- | --- | --- |
| 多图框同模型空间 + 0.01mm 非标单位 | 9 | 条件C 相对面积法 |
| INSERT 块参照图框（×15，缩放约 1.07） | 15 | 块参照识别路径 |
| 布局空间标准 A1 图框；模型空间无图框 | 11 | 布局空间候选 + 误判过滤 |
| A4 加长版图框（长宽比 1.77） | 2 | 条件C ≥√2 允许加长版 |
| 空布局仅含视口 | 1 | VIEWPORT 过滤 |
| 自定义 UCS 整体倾斜（旋转 ≈3.3°，A3 图框） | 1 | 全局旋转矫正 |
| LINE 绘制多图框（密集细节线，3×A1 + 2×A2 + 1×A3） | 6 | 直线矩形长边全配对 |
| LINE 双层封面框（最外 28261×19985）+ 16 张标准图框 | 17 | 直线矩形覆盖排序/比例剪枝 |
| 多尺度套图（块图框 ×4 + LINE 页框 ×6，含 rel 2.9% 小页 + 细长内容块干扰） | 10 | 坐标包含扫描 + 条件C 上限 + 条件D 重复页框 |
| A4 块参照图框；文字外溢 CAD 残框 | 各 1 | INSERT A 系放行 / 降级排除文字 |
| 双线图框（外 594×420 + 内 574×400） | 1 | area_ratio 浮点容差取外框 |
| 图纸目录单页（PUB_TITLE 双层框内含目录表+标题栏块） | 1 | 包裹剔除豁免 √2 纸张边框 |
| 装修平面 2 张并排 + 主框下方孤立小矩形 | 2 | 条件B rel≥10% 覆盖闭合多段线 |
| 效果图套图（4 闭合页 + 3 图框块并排 + 1 闭合小页） | 8 | 对齐白名单 √2 + 条件C 下限放宽 |
| 直线矩形大页框（2×25904×22069 + 3 块图框，非等比拉伸 ratio 1.17） | 5 | 直线矩形条件C 下限放宽至 1.10 |
| 内容级底图大框包页（10 页框块 + 261665×214254 底图，area_ratio 92%） | 10 | rel 分母排除内容级底图大框 |
| 超宽内容排版外框（25 页框 + 532588×132430 底框 ratio 4.02，area_ratio 23.7%） | 25 | rel 分母第二判据（面积 ≥15% 且 ratio >2.5） |
| 多尺度图框块（同块 A4 图框以 ×1/×2×8/×4/×10/×20 插入，尺寸 297×210~5940×4200，跨度 20 倍） | 13 | 条件E 块模板标准幅面 + 同块名族传播 |
| 1:1 设备布置图（全图 149823×77215 ratio 1.94 占 layout 97.6%，图内 653 个设备构件矩形，无纸框/标题栏） | 1 | rel 分母塌缩防护（整图即图框） |
| 布局空间 3×2 网格排版页框（块 434.1×311.1=1.0475×A4 短边 311.1，模型空间另有 5 个内容区块干扰） | 6 | 布局空间网格救援 + 布局空间优先 |
| 同模板缩放套图（3×26305×18450 + 3 个 1:2.5/1:4/1:4.46 小框，六框 ratio 全 =1.4257，小框 rel 6.25%/5.02%） | 6 | 条件G 标题栏条纹签名 + 同模板比例匹配 |
| 同模板四种比例排版 + 虚线内容区边界（16×26000×18000 + 17472/14560×3/10735 小框，21 框 ratio 全 =1.4444；67627×31514 虚线大框包住 1 张真页框） | 21 | 条件H 主导比例内容救援 + 包裹框主导比例剔除 |
| 图框条带 + 独立表格页（4×39662×27975 大框 + 11×27764×19583 中框 + 8 小框 + 19945×15703 图纸目录，目录 ratio 1.2703 非 √2 非主导、无标题栏） | 27 | 条件I 表格内容页救援 |
| 底图参照外框包整版图框（630801×2407287 巨块占整图 76% 包住 8×A0 加长框 + 3×横版框；其中 3 张的图框块为标注版本，bbox 含外溢标签） | 11 | 包裹框剔除块参照强证据 + 同模板短边保护 |

---

## 三、优化记录

### 2026-09-24（第三十五轮 —— 地下室电力t3.dwg 复测 32 框获用户确认"非常精准"：R22 遗留口径确认关闭 + 200700×126150 标准底图跨图互证）

**背景**：R22（9-17）修复本图"漏检 14 张"（5.2 万条 LINE 组合预算耗尽 → 必要条件剪枝），fc 18→32，当时遗留备注"检出尺寸组与用户实测存在口径差异（用户把 84100 主框+左侧 21000 图签附栏整体量成 105100×59400；程序取主框+附栏整体口径），总数一致、待用户页面确认"。

**本轮复测**：用户附图重测（4.3MB、2051 个 INSERT 块）→ **fc=32 全部模型空间**（单图 5m33s，前台 300s 会超时需后台跑）。尺寸分布：105100×59400×11（A1 加长）、59400/84100 系×9（A1 横竖）、59400×42000×5（A2）、118900×84100×2（A0）、200700×126150×2（非标大底图）、126150×89100×1（≈A0 加大非标）、126100×59400×1、57400×80600×1。渲染叠加核验：32 个检出框全部套住含完整内容的图框，无空壳无漏检；#1 右下/#0 左下的不规则暗红轮廓线为边界 polyline（非矩形图框），正确未检。

**用户确认："是32张，非常精准"** → R22 遗留的口径确认正式关闭：主框+附栏整体口径、以及全部非标幅面均获认可。

**跨图互证**：本图 [0][1]（200700×126150 非标大底图真框）与 R34 d012 案例中 321093×259386 真图框**内部嵌套框群尺寸完全一致**（200700×126150 + 内缩 195450×123150）——同设计院标准底图尺寸体系，d012 的"嵌套框群"即该底图在图框内的缩放布置。跨图印证 200700×126150 是真实图框尺寸而非杂线。

**回归**：`regress_check.py --update --files` 复跑本图（5m28s），基线条目（fc=32 / best=200700x126150 / sizes 8 组）重写后**逐字节一致**（git 零 diff）——4.3MB 大图复现性验证通过，基线维持 39 张不变。

### 2026-09-24（第三十四轮 —— d012.dwg 误检 4 框（3 实际）：√2 纸张保护加「空壳范围框」破例）

**需求**：用户验证 `d012.dwg`（配电房大样，二进制 DWG 走 ODA 转档）：实际 3 个图框（模型空间 2 + 布局 1），程序识别出 4 个。多出 **518495×335087**（模型空间闭合多段线）。

**定案**（渲染 2 图 + 插桩）：真图框 = 模型空间 **321093×259386 块参照 ×2**（ratio 1.2379 低于条件C 下限 1.343，靠条件I 表格页救援入选，救对了）+ 布局"配电房大样及接地" 84100×59400 ✓。多出的 [0] 是 **BZ（标注）层的 LWPOLYLINE 4 顶点规则矩形**（ratio 1.5473）——罩在真图框 [1] 外面的"上半页标注范围框"，内部除 [1] 内容外几乎空，罩住 [1] 的 91.6% 面积但 **[1] 底边溢出 [0] 底边 21845mm**（错位半包含）。

**根因**（五道防线逐一实测失效，其中一道"即使去掉也剔不掉"）：
- ① 条件C **rel=100% 自我循环**：[0] 是模型空间最大候选 → 相对面积分母=自己 → rel 必过；ratio 1.5473 ∈ [1.343, 2.5]。area_ratio 仅 0.50%（msp 总包围盒 3.466e13 被远距离散内容撑大，条件A 的 15% 不可达——条件C 本为此场景设计，被范围框利用）。
- ② 孤证复核 ⑥互证：[0] bbox 内含嵌套框群 200700×126150（+内缩 195450×123150，各 2 份）→ score=1.0 保留（SOLO_DEBUG 实测）。
- ③ 包裹剔除 √2 纸张保护擦线：9.41% ≤ 10% → continue。镜像讽刺：真图框 [1] ratio 1.2379 偏离 12.5% 反而在保护外。**插桩进一步证明 √2 保护并非决定性**——即使去掉保护，主判据也剔不掉 [0]：[1] 错位溢出不进 inners；嵌套框群在 [1][2] 框内各一组（same_n=2<3 触发块参照豁免，[1][2] 自身不被剔）；在 [0] 内则与 200700 聚同组 groups=1<2；dom 不中（嵌套框 ratio 与 [0] 仅差 2.8%<10%）。
- ④ 去重双路失效：嵌套剔要求完全包含——[1] 底边外溢 → 不触发；IoU=0.422<0.5——尺寸悬殊对子（[0] 面积=[1] 的 2.09 倍）IoU 天然被大框稀释。

**修法**（方向三"内容稀疏"为主 + 错位护栏，app.py `strip_wrapping_frames` √2 保护段内加破例，**四条全中才直接剔除**）：
- ① 存在候选面积 ∈ [25%, 50%)×big——罩"一张图"的框与图同量级，<25% 的跨边界小矩形是"边缘杂线"形态不触发；
- ② 交集 ≥ 90%×该候选面积——被罩的图几乎整个在框内；
- ③ 该候选未被 big 完全包含（**错位溢出**，防误伤的关键护栏）——真图框的内框/内容候选全部完全包含；
- ④ 内容稀疏：big bbox 内**自由内容线长占比 <10%**（线段两端点均在 bbox 内才计入；排除自身边线——两端点均落在 bbox 四边 ±1mm；排除被同 layout **全量候选** bbox 认领的线段——含未通过特征的小候选，口径与插桩一致，若只用 frame_like 认领 [0] 占比升至 ~12% 翻案）。
- **阈值依据（插桩实测三场景）**：d012 [0] 自由占比 3.82%（稀疏 ✓）；d012 [1] 真图框 0%、图纸目录.dwg 双层框 0%——**纯稀疏判据会把目录框也判成空壳（回归必炸）**，故必须组合错位护栏：目录外框的内部候选全部完全包含（不满足③）、目录内框 vs 外框交集 89.7%<90%（②挡）且面积比 111.6%>50%（①挡），双双安全。线段收集只取顶层实体不展开 INSERT（被罩的图作为候选以 bbox 参与认领，块内线条无需展开）。
- 性能：①②③纯几何先筛（绝大多数候选零成本 continue），④仅对命中者惰性收集线段+按 layout 缓存，预过滤与 big 相交的认领 bbox。

**验证**：后端 **64/64**（新增用例 56：d012 全要素复现——BZ 层范围框 + 2 个表格线块参照真图框 + 真实坐标嵌套框群（[1][2] 各一组 same_n=2 保住块豁免）+ 远距离散实体撑大 area_ratio，修复前 fc=3 红、修复后 fc=2 绿且 [0] 消失）；d012 真图复测 **fc=3**（模型 2 + 布局 1，与用户理想预期一致，破例日志触发：交集 92%、自由占比 3.8%）；38 张基线回归全部 SAME（图纸目录.dwg 双层框零扰动——破例护栏实测不触发）。

### 2026-09-23（第三十三轮 —— D011_t3.dwg 漏检（2 实际 0 检出）：旋转矫正加「轴对齐图框级闭合矩形存在性」豁免）

**需求**：用户验证 `D011_t3.dwg`（t3 转档版）：实际有 2 个图框，程序识别出 0 个。

**定案**（角度直方图 + 渲染 3 图）：2 个真图框 = **59400×42000 闭合多段线 ×2**（A2 横版、轴对齐、孤立在全图 x≈1e6 右上角，外内双线嵌套 55900×40000）；另 40581×16742 = "商铺部分详本子项08号图"文字遮罩框（轴对齐闭合矩形）；图内主体是**铺满全图的 45° 斜向轴网线**。

**根因**（旋转矫正误伤轴对齐图框）：45° 斜向轴网线长占全图 **70.6%**（45°+44° 桶合计，0° 族仅 15.1%），旋转矫正的族占比门限（≥65% 触发）挡不住 → 主峰 45.5° → 整图按 45° 旋转"矫正"——但图框本已轴对齐，被转成 45° 斜置 → bbox 变 71592×71808 近正方形（ratio 1.003 < FRAME_RATIO_MIN 1.05）→ 图框候选全灭 → fc=0。门限设计假设"斜向内容占比高 = 整图斜置"，被"**斜轴网 + 正图框**"的真实组合证伪：轴网是内容不是坐标系。

**修法**（app.py `_estimate_global_rotation` 加轴对齐图框级闭合矩形豁免）：
- 遍历 LWPOLYLINE 取点时顺带探测（**复用既有遍历，零额外扫描开销**）：闭合 + 4 顶点（5 顶点首尾重复归一）+ x/y 各 ≤2 个不同值（1mm 容差，即轴对齐矩形）+ ratio∈[1.34, 2.5] + 短边 ≥2000mm → `_axis_frame_seen = True`；
- 偏离检查通过、即将返回旋转角之前：若 `_axis_frame_seen` → 打豁免日志、**返回 None 跳过旋转**。
- 阈值依据：ratio 下限 1.34 = √2(1.414) 留余量（排除正方形遮罩框误触发）；短边 2000 = 现有图框级短边下限；ratio 上限 2.5 与全链路一致。
- 保留场景：真斜图框（UCS 整体倾斜）不含轴对齐大矩形 → 探测不命中 → 旋转照常生效（用例 55 保护）。

**验证**：后端 **63/63**（新增用例 54：D011 场景复现——2×外内双线轴对齐框 + 20 条 45° 斜线，修复前 fc=0 红、修复后 fc=2 绿；用例 55：UCS 3.5° 真斜图框保护——旋转仍生效 fc=1，角度取 1° 直方图**桶中心** 3.5° 消除量化残差）；D011_t3 真图复测 **fc=2**（59400×42000 ×2 选中，豁免日志触发，遮罩框被嵌套去重收掉）；37 张基线回归全部 SAME。

### 2026-09-23（第三十二轮 —— S015.dwg 误检 4 框（1 实际）：rel 分母排除判据① 加「近√2 页面级候选存在性」豁免）

**需求**：用户验证 `S015.dwg`（建筑/给排水混排单页图）：实际只有 1 个图框，程序识别出 4 个。

**定案**（渲染 3 图 + 插桩逐候选通道核实）：真图框 = **66900×42050 外框 + 65150×41050 内框线**（嵌套双线画法，闭合多段线；ratio 1.591/1.587 **偏离 √2 达 12.5%/12.2% 的非标比例**，area_ratio 0.999/0.9497，右下带标题栏）。4 个误检全是内容：**4941×2582×2** = 给排水"18m³ 消防水箱"等轴测轮廓的两个投影面矩形（部分重叠），**1350×3100×2** = 建筑详图两个并排坑/井轮廓（带标高+轴网）。

**根因**（一条完整的"错位冤案"链，六环相扣）：
- ① rel 分母排除判据①（area_ratio≥0.5 且 ratio 偏离√2>10%）把两大框踢出分母——判据本意踢"非纸张比例的整图排版底框"（一楼大厅），S015 的非标比例**真图框**反而中招；
- ② 分母塌缩到**图签块 TUQIAN2**（3500×8802 竖长表格 ratio 2.51，面积 30.8e6——自身连图框候选都过不了，却成了 rel 分母）；
- ③ 塌缩防护判据"分母占 layout <1%"实测 **1.09%，差 0.09 个百分点**没触发 rel 置零；
- ④ 4 个内容矩形 rel 被放大 91.4 倍（真实 0.45%/0.15% → 41.4%/13.6%），全部越过条件 C 的 10% 门槛误入；
- ⑤ 孤证复核半失效：3200×4500 块（块名"345"，无互证）score=0 被剔 ✓ 评分制正常；但 4 个内容矩形 **same_n=2 同尺寸互证**得分误留；
- ⑥ 包裹框剔除反噬：两大框框内恰含 2 个独立尺寸组（4941×2582×2 + 1350×3100×2）→ 真图框被当成"外包络"剔除。
- 深层机理：分母塌缩误收内容（前置错误）+ 包裹剔除把真图框当包裹框（放大错误），两个机制各自的护栏（评分制、塌缩防护）在 S015 的参数组合下恰好双双失效——继 R31（S010）之后 rel 分母问题的第二种暴露形态。

**修法**（app.py 判据① 加"近√2 页面级候选存在性"豁免，预扫描 + 一行条件）：
- 原设想"ratio≤2.5 一刀切豁免"被**一楼大厅反例证伪**（其外轮廓 ratio 1.22≤2.5，豁免会让它回分母、10 张真页面 rel 被稀释全拒）——两类场景的区分关键是 **layout 里有没有近√2 的页面级候选**：
  - 一楼大厅：有（10 张真页面 ratio 1.4143、area_ratio 最大 3.9%）→ 非标大框是"排版底框"且有稀释对象，踢出分母是对的；
  - S015/运煤胶带机：没有 → 非标大框没有稀释对象（即便有候选也因 area_ratio<2% 必然过不了条件 C 的 10% 门槛），踢出只会造成分母塌缩（S015 fc=4 / 运煤胶带机靠塌缩防护兜底），豁免保留其分母资格。
- 实现：分母排除前预扫描 per layout 的 `_sqrt2_page_exists`（近√2 ±10% 且 area_ratio≥2%，与分母排除段同口径跳过 area_ratio>1.0 异常候选）；判据① 踢出条件追加 `_sqrt2_page_exists.get(layout)` 前提。判据②（ratio>2.5）不动——ratio>2.5 已排除"非标真图框"可能（滨江 25 张微页面场景仍受保护）。
- 阈值 2% 依据实测分离度：一楼大厅页面块 area_ratio 1.2~3.9%（最大者须计入"存在"才能维持保护）vs S015 唯一近√2 内容块 3200×4500 的 0.51%（若计入会维持误踢）——2% 居中留余量。

**验证**：后端 **61/61**（新增用例 53：S015 场景全要素复现——双线非标外内框 + 图签块 TUQIAN2 + 4 个内容矩形 + 框外撑大实体，修复前 fc=4 红（4 个内容矩形 bbox 与真实 S015 逐位一致）、修复后 fc=1 绿（66900×42050 外框，内框 IoU 95.07% 被去重收掉））；S015 **fc=1** 与用户实测一致；36 张基线回归全部 SAME（一楼大厅外轮廓仍被踢/运煤胶带机外轮廓回分母但 fc=1 不变/滨江判据② 照踢，推演回归双确认）。

### 2026-09-23（第三十一轮 —— S010.dwg 漏检（fc=0 但尺寸"看起来对"）：R27 计回条件加图幅尺寸上限，阻断巨型底图污染 rel 分母）

**需求**：用户验证 `S010.dwg`（总平面图）：页面显示图框数 0，但尺寸是对的——为什么？

**现象本质**：smart 模式下解析**直接抛"未检测到图框"**；前端按设计走 force_max 降级重试链路，返回全实体包围盒尺寸（所以"尺寸对"）但无候选（所以 **fc=0**）。前后端行为均符合设计，错在 smart 识别层。

**根因**（候选普查 + 分母追溯）：真图框是 **105100×59400 闭合多段线**（A1 加长 1050 图幅、1:100 出图，ratio 1.769）。漏斗逐环节：
- msp 唯一 xref 巨型底图"北区商业街"（块 `09-AW-FP001-PJ$0$北区商业街2013-06-03`，**6345273×1806446**，即 6.3km×1.8km）被 R27 闭合通道拒（闭合证据 352362mm=5.6%<50%）；
- 但其块内含超长道路线/嵌套子块，`_block_max_member_len` ≥ 块长边×30% → **构件证据"合格" → R27 修复逻辑把它计回 rel 分母**（`_r27_rej_max=1.146e13`）；
- 真图框 rel = 6.24e9/1.146e13 = **0.054%**，条件 C 的 10% 门槛永远不可达；条件 B 短边 59400>2000 不可达；A 的 area_ratio 同样被底图污染（0.054%<15%）；G/H/I 均不命中 → frame_like 空 → 抛"未检测到图框"。
- 注：R26 时代该底图构件合格同样会入库成为分母 → **此图在历史版本同样检不出**，非 R27 回归，属"巨型底图污染分母"类问题首次被验证暴露。

**修法**（app.py R27 计回条件加图幅尺寸上限，一行级）：计回条件追加 `max(_w, _h) <= _R27_CREDIT_MAX_SIDE(2.5e6mm)`。依据：真图框长边物理上限 ≈ A0 纸 1189mm × 1:1000 出图 ≈ 1.19e6mm，2.5e6 留两倍余量；S010 底图 6.3e6 超限 2.5 倍被排除。与 R27"构件不合格不计回"（1号2号楼 S-0-COLS 1.5e12）并列构成计回的双重防护：**构件合格且尺寸在图幅合理范围内**才计回。

**验证**：后端 **60/60**；S010 **fc=1**（105100×59400 选中，2 个内框被去包裹剔，与推演逐位一致）；35 张基线回归全部 SAME。

### 2026-09-23（第三十轮 —— 坡道大样6月.dwg 误检 1 框（"机动车库"轮廓）：条件 D 新增内容证据）

**需求**：用户验证 `坡道大样6月.dwg`：实际 12 个图框，程序检出 13 个，多出一个 4700×6000。经渲染核对定案为**误识别**——它是平面图上标注"机动车库"的房间轮廓线（4.7m×6m 车库平面尺寸，紧邻柱网轴线与柱符号，无页面边界无图签）。用户所数 12 框 = 10×42050×29700 页框 + 21020×11880 + 16820×11880 页内小框，完全吻合。

**根因**（插桩实测）：图内有 **3 个同尺寸 4700×6000 直线矩形**（不同详图里重复画的同款车库轮廓），全部经**条件 D（同尺寸≥2 的套图小页框救援）**放行——条件 D 三道防误收约束恰好全部失效：①ratio 1.2766 离 √2 偏差 9.73%，**擦线进入 ±10% 容差**；②rel 2.26% ≥1% 门槛；③同尺寸 3 份 ≥2 满足"刻意重复排版"语义。条件 D 的设计假设"真页框以外极少有同尺寸 √2 比例矩形刻意重复 ≥2 次"被证伪：**房间/车库轮廓同样会同尺寸重复**。后续各道清洗均拦不住：孤证复核只查条件 C 通道候选（D 进来免检）、不在任何页框内（包裹剔除不管）、3 个实例去重剩 1。

**修法**（app.py 条件 D 新增内容证据，"加特征"路径再次落地）：条件 D 判据追加"框内**严格内部**完全包含实体 ≥ `COND_D_MIN_ENTITIES(12)`"。实体收集口径与条件 H 一致（同白名单），判定窗口边界内缩 1mm（`COND_D_INNER_TOL`）——**排除候选自身 4 条边线**（±1 容差会把边线算进去，空框也有 4 实体）。惰性缓存 per layout（`_cond_d_entity_count`），无 D 候选的图零开销。

**阈值依据**（实测分离度极端）：电子称皮带输送系统 D 通道真页框 14894×10531×2 框内实体 **517/485** 个 vs 坡道大样内容框 **0/0/1** 个——两个数量级以上差距，12 与条件 H 阈值对齐且留足余量。

**验证**：后端 **60/60**（新增用例 51 同尺寸空轮廓框不救援 fc=0、用例 52 框内有内容的同尺寸页框保留 fc=2——两者构造"大框撑 rel 分母+孤证剔"场景覆盖 D 判据；**用例 40 同步修订**：4 个 39598×28000 页框原构造无内容、走 D 通道，补每框 4 个内部小矩形（16 实体），更贴近真实柱网图）；坡道大样 **fc=12** 与用户实测一致，通过特征 33→24（D 拦掉 4700×6000×3 + 8000×6000×4 + 7600×5000×2 共 9 个内容框）；34 张基线回归全部 SAME（电子称图 D 真页框保持）。

### 2026-09-23（第二十九轮 —— 13013-11-AW-FP.dwg 漏检 1 框：孤证互证⑤扩词「图幅代号」（评分制首个按"调权重姿势"落位的新案例））

**需求**：用户验证泛悦国际北区地下室 `13013-11-AW-FP.dwg`：实际 6 个图框，程序检出 5 个。

**根因**（插桩 + 实体普查 + 渲染叠加定案）：漏检的是块 **`A1+0.25`** 的独立图纸（157650×89100，A1 长边加长 1/4 图幅、1:150 出图比例，框内户型平面+楼梯大样+图签栏完整，渲染见 tmp_fp_full/zoom）。漏斗全程：入库 ✓ → 特征匹配 ✓（12 个通过特征之一，rel=55.5% 过条件 C，area_ratio=0.33%<5%）→ **孤证复核六通道 score=0 被剔**。逐条核实六通道全灭原因：①全图唯一无同尺寸副本；②ratio 1.769 与页框 1.591/A1 1.416 均不相近；③**块内明明有完整图签但 title_stripe=False（条纹特征检测盲点）**；④内部无条纹子候选；⑤**块名 `A1+0.25` 是标准图幅命名但关键词表只认 `图框`/`frame`**；⑥内部无通过候选。深层：孤证复核"真图框必有互证"假设被此图证伪——独立摆放的图框天然无副本无兄弟，本应兜底的 ③⑤ 恰好双双失效。注意 score=0，调权重救不了（0×任意权重=0），必须新增证据通道。

**修法**（app.py 互证⑤扩词——评分制"加特征"路径的首次落地）：`_SOLO_WEIGHTS['name_declared']` 的命中条件从"块名含 图框/frame"扩展为"块名含 图框/frame **或图幅代号 A0~A9**"（`_SHEET_SIZE_NAME_RE = (?<![a-z0-9])a[0-9](?![0-9])`）。词边界防护：`(?<![a-z0-9])` 防 ACAD 匿名块名（`A$C6A6C4DA6` 的 a6 前是字母数字，无词边界不命中）；`(?![0-9])` 防 `A31005` 类图纸编号误伤。命中时 hits 输出 `⑤块名图幅(A1)` 便于排查。

**验证**：后端 **58/58**（新增 2 用例：④9 `A1+0.25` 独立图框经⑤图幅代号救援保留、50 匿名块名 `A$C6A6C4DA6` 孤立尘埃仍被剔防误伤——两用例均确认走孤证剔除路径，修复前一红一绿）；目标图 **fc=6**（3×200700 页框 + 157650×89100 + 2×84100 A1）与用户实测一致，评分日志 `[孤证评分] 157650x89100 保留 score=1.0（⑤块名图幅(A1)）`；33 张基线回归全部 SAME（零回归）。

**遗留**：③标题栏条纹为何未检出该块图签（条纹检测盲点）——待后续真实案例再评估是否调参（有 R27 式误伤风险，需数据支撑）。

### 2026-09-23（第二十八轮 —— 孤证复核证据评分制试点：六通道布尔逻辑 → 加权评分（等价重构，行为逐位不变））

**背景**：架构评审确认项目存在 3 个结构性风险（阈值逐案拍定、顺序承载正确性、隐式假设累积），其中"条件逐案加 if 分支"的演进方式在 R27 回归期间暴露得最充分——互证资格限制（area_ratio/尺寸/类型下限）两次被全量插桩数据证伪（32 张基线 413 条救援样本证明尘埃与真框在单维特征上不可分：420mm 的 A3 真框 < 尘埃 957mm；滨江 11093×8535×15 弱+弱真框副本对 vs 一层.dwg 960×2400 门副本）。结论：未来新案例的正确演进路径是"调权重 / 加特征"，而不是"加 if 分支"。

**修法**（app.py 孤证复核段等价重构，作为评分制的第一个试点段）：
- 六通道互证布尔逻辑（原 `_corr` 标志 + 任一命中即救援）改写为加权评分：**score = Σ wᵢ·fᵢ**（fᵢ∈{0,1}），`score < _SOLO_SCORE_THRESHOLD(0.5)` → 孤证剔除。六通道对应 6 个权重键，集中于 `_SOLO_WEIGHTS` 表：①`same_size_copy`（同 layout 同尺寸副本）②`same_ratio_sibling`（同比例兄弟 ±1%）③`title_stripe`（自带标题栏条纹）④`stripe_inside`（内含带条纹子候选）⑤`name_declared`（块名含 图框/frame）⑥`nested_frame`（内含不同尺寸通过候选）。
- 评分收敛为纯函数 `_solo_evidence_score(_c)`，返回 `(score, hits)`——hits 为命中证据明细，供调试日志输出。
- 新增开关 `FRAME_PARSER_SOLO_DEBUG=1`：保留分支逐候选输出 `[孤证评分] WxH 保留 score=…（证据明细）`，替代原先"只知结果不知理由"的黑盒状态。
- 触发条件（`_via=='C' && area_ratio<0.05`）、剔除汇总日志、历史场景注释全部保留原样。

**等价性论证**（行为逐位不变的依据）：
- 初始权重全部 1.0、阈值 0.5 → `score > 0 ⟺ 原 _corr=True`：任一证据命中即过阈值，与"任一互证即救、无互证则剔"一致。
- 原布尔逻辑的短路语义（命中即停）与求和语义在正权重下等价：多证据叠加只增大分值，不改变保留结论；①②⑥ 各自命中后 break 保留（省时且语义不变），④ 改为无条件检查只增分值不改结论。
- 唯一结构差异是求值方式（标志位 → 累加分），不涉及任何候选增删顺序变化。

**验证**：后端 **56/56**（含孤证复核直接消费者用例 41/42/43/48，用例 48"同尺寸副本对经互证①保留"正路径保持）；**33 张基线回归全部 SAME（零 DIFF 零 NEW）**——等价重构的硬验收。附 `FRAME_PARSER_SOLO_DEBUG=1` 冒烟（地下室电力t3，5m47s）：51 条评分日志，score 与命中证据一目了然（如 `118900x84100 保留 score=5.0（①同尺寸 直线矩形；②同比例…；③标题栏条纹；④内含条纹子…；⑥内含…）`），73600×57400×11 真框副本对经①②救援路径清晰可读，fc=32 与基线一致。

**评分制演进路径**（后续轮次按数据决定是否推广）：
- 若试点稳定，逐步推广到条件 A~I 特征通道（同样的"特征层 → 评分层 → 决策层"三段式）。
- 权重分级调优只改 `_SOLO_WEIGHTS` 表，不动逻辑代码：如 ③④⑤ 定为强证据（权重 ≥ 阈值，单独成立即救）、①②⑥ 依伙伴类型/副本数细分权重——每次调整用 33 张基线回归验证。

### 2026-09-22（第二十七轮 —— 拼接图误检 2 假框 + 真页框被剔：R26 内容块校验升级「闭合边框证据」通道）

**需求**：用户指出 `拼接图.dwg`（泛悦国际/建筑/08.09）实际只有 3 个图框，程序检出 5 个。

**根因**（渲染叠加 + 实体级诊断，因果闭环）：
- 真 3 框 = 上下 2 个拼接页框（内外双线 245250×126150 + 240000×123150，各拼 2 张平面图）+ 1 个 178350×126150 总图页框。
- **误检 2 个**：页内平面图内容块 9ZZZW（块定义 71846×50889，108 条 LINE + 6 条 LWPOLYLINE + 17 个天正代理件），rel 11.8% 恰过条件 C 门槛直接混入。
- **漏剔真页框 2 个**：上/下页框内部恰各含 2 个同尺寸 9ZZZW 实例 → 触发包裹框剔除（MIN_WRAPPED_SAME_SIZE=2）把 245250 真页框剔掉（178350 页框因内部仅 1 个 9ZZZW 逃过）。即 9ZZZW 误入候选**既直接误检、又诱发真框被剔**。
- **R26 判据拦不住的原因**：9ZZZW 最长直边构件 45317mm = 块长边 63%，远超 R26 的 30% 门槛——平面图轮廓线本来就长，单看"最长构件"无法区分图框与平面图。

**修法**（app.py 升级 R26 校验为双通道，`_block_closed_frame_len` + `_BLOCK_CLOSED_RATIO=0.50`）：
- **主判（R27 闭合边框证据）**：真图框块必有「闭合矩形边框」，计算块内闭合证据长边，< 块长边 × 50% → 内容块剔除。闭合证据三来源取 max：① 闭合 LWPOLYLINE 的 bbox 长边（首尾点重合但忘设 closed 标志的也视同闭合）；② LINE 横竖线配对成环——横线（|dy|≤tol_p 且长 ≥30% 块长）按 y 聚类、竖线按 x 聚类（tol_p = max(1mm, 块长×0.2%)），两横簇（y 距 ≥30% 块长、x 重叠 ≥30% 且覆盖短者 80%）+ 两竖簇支撑（x 落在重叠带内、y 覆盖矩形高度 ≥80%）判成环；③ 嵌套子块闭合证据 ×缩放递归。
- **辅判（R26 构件证据，降级保留）**：主判通过但最长直边构件仍 <30% → 碎线拼框剔除，日志区分两种原因。
- 平面图内容块的轮廓线是开放散线，单条再长也凑不成闭合矩形 → 9ZZZW 闭合证据 = **0mm** 被剔。

**验证**：拼接图 **fc=3**，构成 245250×126150×**2** + 178350×126150×**1** 与用户实测完全一致（9ZZZW×5 + 9SYZW 全被闭合通道剔除 → 页框失去内含同尺寸组不再触发包裹剔除 → 240000 内框被 245250 外框 IoU 剔、173100 被 178350 嵌套剔，与推演一致）；后端 **55/0**（51 旧 + 新增 4：LINE 成环块放行 / 长散线无闭合块剔 / 首尾重合开放多段线放行 / 稀疏散件场景回归）；31 张基线回归全部 SAME（零回归）。附带修正：单测用例 45/47 明确「入库即剔 → 0 候选 → 全实体包围盒降级」与「入库后孤证复核剔 → 抛未检测到图框」两条路径的差异，断言统一为无『块参照插入』类型候选。

**回归事故与修复（同轮续，一层.dwg fc 0→11）**：
- **因果链**：R27 全量回归发现一层.dwg（用例 41 原型图，实证 0 图框）fc 0→11。R27 闭合通道把其标注内容块 A$C04810A0A（12240×8770，块内闭合证据仅 5170mm=42.2%<50%）剔出候选 → 条件 C 的 rel 分母塌缩（1.073e8→2.06e7）→ 原本被 10% 门槛压住的家具级矩形（957~5160mm 门/窗/洁具轮廓，R26 时代 rel 仅 1.9~9.7%）全部涌入条件 C，并在孤证复核靠互证①②⑥（家具尺寸间的巧合同尺寸/同比例/嵌套）逃生 11 个。
- **互证资格路线被数据证伪**：先试 area_ratio≥0.4% 救援下限，冒烟即翻车（6、10号楼 fc 20→2、自贡19 19→0——离群实体把 msp_total 撑爆，真图框 area_ratio 也低到 0.004~0.11%）。随后在互证①②⑥插桩跑全量回归，收集 32 张基线图共 413 条救援样本逐维对比：尺寸（二层平面图 420×297 的 A3 真框 < 尘埃 957mm）、类型（尘埃 16 条里 8 条闭合多段线=强类型；真框救援 65 条直线矩形，弱+弱同尺寸副本对如滨江 11093×8535×15、地下室电力 73600×57400×11 全是真框）、area_ratio（DS4 真框 0.004% < 尘埃 0.033%）、rel（10~100% 全交叉）、长宽比（家具比例也恰为 √2）——**所有单维特征全部重叠，互证资格判别整体不可行**。
- **最终修法（rel 分母计回，恢复 R26 时代分母状态）**：R27 闭合通道拒绝的块中，**构件证据合格者**（最长直边构件 ≥ 块长边×30%，即"若非闭合拦截本可通过 R26 入库"的块，如 A$C04810A0A）其面积按 layout 记账、取 max 计回 rel 分母（`_r27_rejected_max_area`，app.py 入库校验段 + rel 计算段）。R26 时代这类块本就入库成为分母，计回即消除塌缩。构件也不合格的巨型内容块（1号2号楼 S-0-COLS 1.5e12、6、10号楼 -1fxhs0421 1.5e10）R26 时代同样被拒、从未进过分母，**不计回**——若误计回会把真框 rel 全压死（42000 框 8.6%<10%）。取 max 计回天然自限：无塌缩的图零影响。
- **修复后一层.dwg 行为与 R26 基线逐位吻合**：计回后 16 个尘埃中仅 5160×2730（rel 13.1%）过 C，其同比例伙伴 1160×2180（rel 2.4%）被压死 → 无互证 → 孤证复核剔除 → fc=0 → "未检测到图框"。
- **验证**：后端 **56/56**（用例 48 由"尘埃副本不互证"改为正路径断言"同尺寸副本对经互证①保留"——资格下限预期被数据证伪，微型无框图纸两副本误检 2 框记为已知局限，真实基线 32 张无此场景）；拼接图 fc=3 保持；**32 张基线回归全部 SAME**（一层.dwg 恢复；拼接图为 [NEW] 待补录）。

### 2026-09-22（第二十六轮 —— 住宅户型构成纠偏：内容块剔除（边框证据校验），检出构成与用户实测清单完全一致）

**需求**：用户核对第二十五轮检出构成后给出权威清单：42000×29700×**2** + 59450×42050×12 + 84100×29700×2 + **26760×16820×2** + 74300×42050×2 = 20 框。与 R25 检出（42000×**1**、无 26760，多出 105800×143285×2 + 斜放 143285×105800×1）不符。

**根因**（渲染叠加图 + 实体级诊断证实）：
1. **块 -1fxhs0421 不是图框**：块定义 bbox 143285×105800（图框级、ratio 1.354）由稀疏散件撑出——块内 56 条多段线全是 200~279mm 的 S-辅助层小方块，**最长单条直边仅 279mm（块长边的 0.19%）**，其余 224 个为天正代理实体；渲染证实其 3 个实例覆盖区基本空白。R25 修复时把它当成了斜放图框（还专门做了去旋转口径），实为「内容块」假图框。
2. **内容块霸占 rel 分母**：其面积 1.5e10 为 layout 最大 → 同图真框的 rel 被压到阈值之下：第 2 个 42000×29700（(850741,27046)，PUB_TITLE 层闭合多段线）rel=8.2%、两个 26760×16820（A-ANNO-TTLB 层，两 84100 框之间）rel=3.0% 均 <10% 被条件 C 拒——即 R25 的"20 框"是 3 个错的换了 3 个真的。

**修法**（app.py 新增 `_block_max_member_len` + INSERT 入库「边框证据」校验）：
- 对通过尺寸预筛的图框级 INSERT 候选，计算块定义内「最长直边构件」（LINE 长度、LWPOLYLINE 边段/闭合 bbox 长边、嵌套 INSERT 子块 bbox 长边，含缩放递归）。
- 最长构件 < 块定义 bbox 长边 × **30%**（`_BLOCK_EVIDENCE_RATIO`）→ 判为内容块不入候选库（真图框块的边框线必然接近全尺寸，80~100%）。
- 本图剔除 48 个内容块实例（-1fxhs0421×3、基础梁 jiqz×10、gzz×8、hxt1×4 等）。剔除后 rel 分母从 1.5e10 回落到 3.124e9（74300 框），三个被压真框自然浮出（rel → 39.9% / 14.4%）。

**验证**：目标图 fc=20 构成与用户清单**逐组一致**（59450×12 ✓ 74300×2 ✓ 84100×2 ✓ 42000×2 ✓ 26760×2 ✓）；后端 51/0；31 张基线回归**全部 SAME（零回归）**，目标图构成变化（105800×143285×2+斜放×1 → 26760×2+42000×2）为预期效果，已更新基线。渲染叠加核对图（r25v2_full/row_top/row_bot/right.png）显示 20 个红框与实际图框线全部贴合。

**附注**：R25 为斜放块做的三项修复（去旋转口径、同尺寸错排豁免、规则1 跳过 tilted outer）保留——逻辑本身正确（真斜放图框仍需此口径），仅本图的"斜放块"实为内容块、被本轮校验前置剔除，不再进入后续流程。

### 2026-09-17（第二十五轮 —— 6、10号楼住宅户型.dwg 漏检 15 张：斜放块 bbox 膨胀三连击 → 去旋转口径 + 错排豁免 + 嵌套跳过）

**现象**：图纸应 20 框（用户清单：59450×42050×12 + 74300×42050×2 + 105800×143285×2 + 斜放框×1 + 84100×29700×2 + 42000×29700×1），仅检出 5。用户特别注明右上角蓝色表格群不属于图框。

**蓝色表格群核实**（渲染 tmp_r25_topright.png 证实）：右上角蓝色表格是 74300 图框内/邻位的门窗表/详图表，尺寸远小于图框级，从未进入候选库——无需特殊排除，属正常过滤。

**根因**（四层，前三层为"斜放块 bbox 膨胀"三连击）：
1. **rel 分母虚增**：图框块 -1fxhs0421（块定义 143284.78×105800，ratio 1.354）某实例 rot=125°，其 bbox 为旋转外接矩形 **168851×178056**（面积膨胀 1.98 倍）。膨胀框 area_ratio 仅 2.69%，躲过 rel 分母排除判据（≥50% 才踢）→ 成为 layout 最大面积 ma=3.0e10 → 12 个 59450×42050 真框（2.498e9）的 rel 被压到 **8.31% < 10%** → 特征条件 C 全拒（fc 20→5 的主因）。
2. **斜放框本体漏检**：膨胀 bbox 使 ratio 失真 1.354→1.055，低于条件 C 的 `_at_least_sqrt2`（1.343）→ 斜放框自身也被拒。
3. **同尺寸错排误剔**：同列上下两张 105800×143285 框纯 y 向错位 19781mm（13.8%），IoU=75.7% > 50% 被去重规则2 误剔（剩 1 张）。
4. **嵌套误剔**（修复①②后新暴露）：斜放框膨胀 bbox (1437785,-196182)-(1606636,-18126) 作为 outer，通过去重规则1（嵌套）误剔了落在其 bbox 角落（而非真实斜带足迹）的邻位真框 74300×42050×1 与 59450×42050×1（fc 18→20 的最后一步）。

**修法**（app.py 三处）：
1. **INSERT 斜放块去旋转口径**：`rot % 90 ≠ 0` 的块参照候选，width/height/area 改用「去旋转真实图幅」（块定义尺寸×插入缩放，旋转不改面积）——本例斜放框按 **143285×105800** 输出；bbox 保留膨胀框（面积/比例/rel 分母全部回归真实）。
2. **同尺寸错排豁免 `_same_size_offset_dup`**：去重规则2 中，宽高一致（含互换，2% 容差）且中心错位 ≥5% max(ow,oh) 的两候选视为"错排独立图"不剔除。真重复画法（中心偏移 ≈0）不受影响；内外框（74300/72550 差 2.4%）尺寸超容差照旧剔除。
3. **tilted 标记 + 规则1 跳过**：斜放块候选打 `tilted: True`；去重规则1（嵌套）跳过 tilted outer——其 bbox 是旋转外接矩形（膨胀框），真实足迹只是框内斜带，bbox「包含」≠真实包含。

**验证**：目标图 **fc=20**，构成与用户清单逐组一致（59450×42050×12 ✓ 74300×42050×2 ✓ 105800×143285×2 ✓ 143285×105800（斜放框去旋转口径）×1 ✓ 84100×29700×2 ✓ 42000×29700×1 ✓）；去重剔除明细仅剩正常内框嵌套（72550→74300、57700→59450、39000→42000）；后端 51/0；31 张基线回归**全部 SAME（零回归）**，目标图 fc=20 已补录基线（现 32 张）。附带：regress_check.py `--update --files` 修正为增量补录语义（此前会清空整个基线只留指定图；目录新增图纸补录不再需要全量重跑 17 分钟）。

### 2026-09-17（第二十四轮 —— 工程化三项：回归基线入库 / DIMASSOC 刷屏抑制 / 日志分级）

**1. 回归基线快照入库**（regress_check.py + regression_baseline.json）
- 痛点：此前每轮回归要导出 HEAD 副本 + 当前版**双跑** 31 张（ODA 转换占大头），耗时长、操作繁琐。
- 修法：基线（fc + best + 尺寸分布）存 `regression_baseline.json` 入仓库；`regress_check.py` 单跑当前版本自动逐图 diff（SAME/DIFF + 差异明细），一致退出码 0。`--update` 显式重建基线（须先人工确认 DIFF 符合预期），`--files a.dwg,b.dwg` 支持冒烟子集。回归时间**减半**，HEAD 副本导出操作取消。

**2. DIMASSOC 控制台刷屏抑制**
- 现象：解析含关联标注的图纸时，PyCharm 控制台反复出现 `copy process ignored DIMASSOC(#xxxx) - this may cause problems in AutoCAD`。
- 根因（两轮排查定位）：消息**不是 ODA File Converter 发的**，而是 ezdxf 自己——`ezdxf/entities/dictionary.py` 的字典深拷贝对无法复制的 DIMASSOC 实体发 `logger.warning`（'ezdxf' logger 无 handler → `logging.lastResort` 直写 `sys.stderr` → 控制台）。实测证据：ODA 子进程 stdout/stderr 管道均为 0 字节、dup2(fd2→DEVNULL) 也拦不住（Python 层输出不走 fd2）。
- 实质：DIMASSOC 只是"标注与图形的关联关系"元数据，标注几何完整、图框解析不用它——**无害告警**。
- 修法：app.py 把 'ezdxf' logger 挂上同一 RotatingFileHandler，`_EzdxfDemoteToDebugFilter` 将其记录降级为 DEBUG，`propagate=False` 切断 lastResort。默认文件和控制台都不再出现；排查期设 `FRAME_PARSER_LOG_LEVEL=DEBUG` 可在文件中查看。
- 验证：连续 3 次解析 1号2号楼柱平法施工图 stderr 0 行、fc=11 稳定；51 项后端单测通过。

**3. 日志分级（parser.log 体积降约 80%）**
- 痛点：统计 parser.log 53361 行中约 80% 为逐候选明细（候选列表、去重剔除明细等排查期临时日志），6 天撑到 8.4MB。
- 修法：新增 `log_debug()`——逐候选明细（最终候选列表、未检测到图框时的全量候选列表、去重剔除明细、附栏外扩逐框明细）降为 DEBUG：默认不写文件不刷控制台；`FRAME_PARSER_LOG_LEVEL=DEBUG` 时恢复写文件+刷控制台。过程摘要（检测结果头尾、✅选中、XREF/未检出告警、各救援条件摘要）保持 INFO。
- 附带：删除旧 parser.log.1（10MB，含 1.4 万条历史 DIMASSOC 重复行）。

**验证**：后端 51/0；解析结果与第 23 轮完全一致（日志/日志级别不影响任何检测逻辑）；31 张基线快照生成后 `regress_check.py` 对比全绿。

### 2026-09-17（第二十三轮 —— 图框口径切换：主图框+图签附栏整体输出（105100口径））

**需求**：用户确认图框应框住"主图框+附栏"整体（第二十二轮遗留口径决策）：地下室电力t3.dwg 中 84100×59400 主图框 + 左侧 21000 图签附栏按 **105100×59400** 输出。

**几何真相**（渲染+实体级诊断确认）：该模板的附栏**不形成独立闭合边界**——J-图框/TK 层图签条（约 7000×18220 表格）画在主图框边线内侧右缘；用户所见"左侧附栏"实为**错排布局中相邻同排图纸的图签条**（两行图框 y 向错开 ~2700，行间横向净距 1.0~1.6 万，邻框图签条恰好落在本框左侧 21000 邻位）。全图 32 个图签条簇与 32 个检出框一一对应，其中 8 张横版主框左侧 2000~22000 邻位存在"同排同规格图纸内部的图签条"→ 外扩后与 3 张已按 105100 闭合检出 + 1 张 126100 的框合计，尺寸组与用户实测清单一致。

**修法**（app.py 新增 `_collect_title_strips` / `_expand_frames_with_title_strips`，接入点在去重之后）：
1. 收集 J-图框/TK 层实体 bbox，2m 间隙聚类，筛"图签条"尺寸（短边 4000~13000 且长边 ≥14000）。
2. 对每个横版主图框候选（短边 ≥50000 且宽 <95000），若其左侧/右侧 2000~22000mm 邻位存在图签条，且：
   - 图签条与主框纵向重叠 ≥40% 条高（错排容差）；
   - 图签条"宿主框"（条中心所在候选）≠ 主框自身，且宿主高度 ≥0.85×主框高度（同排同规格；宿主为 42000 小页等不同规格 → 是小页拼排不是附栏）；
   则该侧外扩 **21000**，同步更新 bbox/width/height/area/ratio。开关：`FRAME_PARSER_NO_TITLE_STRIP=1`。

**验证**：后端 51/0（新增 8 用例：聚类识别/自身条不外扩/错排外扩 21000/竖版不扩/小页不扩/已含口径不重复扩/异规格宿主不扩/端到端 105100+84100）；31 张全量回归（HEAD 基线副本）**一致 30 / 变化 1**，唯一变化即目标图口径切换（fc 32 不变，84100×14 → 105100×11 + 84100×6），其余 30 张尺寸明细逐字节一致。地下室电力t3.dwg 检出：**105100×59400×11 + 84100×59400×6 + 59400×84100×3 + 57400×80600×1 + 59400×42000×5 + 200700×126150×2 + 118900×84100×2 + 126150×89100×1 + 126100×59400×1 = 32**（用户清单中 105100×11、126100×1、42000×5、118900×2、126150×1、200700×2 六组精确一致；84100×4 vs 6、59400×84100×6 vs 3+1 两类归属差异待用户复核——涉及竖版内框 57400×80600 的取框层级问题，属下一轮）。

### 2026-09-17（第二十二轮 —— 地下室电力t3.dwg 漏检 14 张：直线矩形组合预算耗尽 → 必要条件剪枝）

**现象**：图纸应 32 框，仅检出 18。与 1号2号楼（图框在冻结图层）不同，本图全部图框线在**可见图层**（TEL_TITLE/TB/BZ），且四条边线完整存在——如 TEL_TITLE 层 84100×59400 框（x -1002863..-918763）边线齐全却未拼出。

**根因**（两层，均在本轮修复）：
1. **组合预算耗尽**：本图 5.2 万条 LINE → 横/纵长边聚类 8164/4931 个 → "长边全配"通道产生 **109 万横线对 × 51 万竖线对**。真实图框的横线对虽按公共覆盖降序排 #569，但此前 572 万次无效检查把 `MAX_EXAMINED=200万` 组合预算耗尽在 ~#200 → 后续图框线对永远轮不到（与长中苑/电子称同类故障的新形态：排序已优化，但预算仍不够）。
2. **浮点尾差陷阱**：第一版剪枝用严格 `<`，TB 层 126100×59400 框的竖对覆盖总长与横对高度理论相等、实际差 ~2e-8mm → 反被误杀。剪枝必须留容差。

**修法**（app.py `detect_rectangles_from_lines`）：预计算每个竖线对的公共覆盖总长 `v_covs`；主循环内在 `examined += 1` **之前**，`v_covs[vi] < 矩形高 - 2mm` 直接跳过（必要条件：竖对覆盖不住矩形高就绝不可能成框）。目标线对前检查量 **572 万 → 41.5 万**，预算内可达，零召回损失。

**验证**：后端 43/0；fc 18→32（数量与预期一致）；30 张全量回归仅目标图变化。检出尺寸组与用户实测存在口径差异（用户把"84100 主框+左侧 21000 图签附栏"整体量成 105100×59400；程序取主图框），总数一致、待用户页面确认。

### 2026-09-17（第二十一轮 —— 图纸名称列双行结构化：文件名主行 + 目录副行）

**需求**：使用文件夹路径显示时，名称列单行输入框截断长路径，一眼看不到完整路径；要求行高保持和谐。经三方案对比（双行结构化 / 加宽列+悬停 / 完整换行）选定双行结构化。

**修法（纯前端 app.js + style.css）**：
1. 新增 `splitNamePath(name)`：按最后一个 `/` 或 `\` 拆分完整路径为「目录（含结尾分隔符）+ 文件名」，无分隔符则整段为文件名。
2. 名称列改双行渲染：主行 `<input>` 只显示**文件名**（加粗、可编辑），副行 `.name-dir` 灰色小字显示**所在目录**（超长省略号，悬停 `title` 显示完整路径兜底）。所有带目录的行统一两行高度，视觉和谐。
3. 改名语义调整：编辑框只改文件名段，保存时自动拼回原目录（用户输入含路径分隔符时视为完整路径原样保存）。`rec.name` 内部始终存完整路径——CSV 导出、生成删除脚本的 keepSet 匹配均不受影响。

**验证**：前端 7 文件 **222 项全绿**（`test_single_frame_tip.js` 新增第 12 组 11 项：路径拆分 5 + 改名拼接 3 + 源码护栏 3）；后端无改动。

### 2026-09-15（第二十轮 —— 混合筛选拆两档：⚠️ 混合·强 / 🌀 混合·弱 分开筛选）

**需求**：混合徽标本就分两档（weak=多图框且全部标准"多张标准页"；strong=标准+非标并存/多种非标并存），但筛选面板只有一个"混合"选项——两档对用户意义不同，需要分开筛选。

**修法（纯前端）**：
1. 筛选面板"混合"拆为两个选项：`⚠️ 混合·强（标准+非标/多类型并存）`（value=mixed_strong）与 `🌀 混合·弱（多张标准页）`（value=mixed_weak），默认均勾选（与原单一选项行为一致）。
2. `recordMatchesFilter` 混合维度按 `mixedLevel(rec)` 分档命中——**两档互不覆盖**（勾"混合·弱"不会带出 strong 记录，反之亦然）；未勾选时混合记录仍按主导类型参与类型过滤。
3. 导出文件名后缀同步两档标签（混合强/混合弱）。

**验证**：前端 7 文件 **211 项全绿**（`test_single_frame_tip.js` 新增第 11 组 14 项：mixedLevel 等价 4 + 两档匹配 7 + 源码护栏 3；其中"两档互不覆盖"断言首版写反已修正）；后端无改动。

### 2026-09-15（第十九轮 —— 原始文件清单持久化：刷新页面后可直接生成删除脚本）

**现象**：刷新页面后点"生成删除脚本"提示"尚未建立文件清单"，必须重新拖入文件夹完成批量解析才能生成——费时且不必要。

**根因**：删除脚本的待删除清单 = `scannedFiles（原始文件全集）− keepSet（保留记录）`。`records` 有 localStorage 持久化，但 `scannedFiles` 只存在内存（每次拖入/选择文件夹时扫描填充），刷新即丢。且**不能用记录反推全集**——批量停止/部分失败时已解析记录 ⊊ 扫描全集。

**修法（纯前端 app.js）**：
1. `scannedFiles` 持久化到 localStorage（新键 `dwg_scanned_files`，与记录键分开存）：`parseFiles` 扫描入集后立即 `saveScannedFiles()`；初始化时 `loadScannedFiles()` 恢复。
2. 生成脚本确认框新增**清单来源提示**：展示清单规模与最近更新时间；若目录在清单更新后有增删，提示重新拖入解析一次以刷新清单（防陈旧清单误删）。
3. "清空记录"按钮同步清空文件清单及其存储（确认弹窗文案同步说明）。

**验证**：前端 7 文件 **197 项全绿**（`test_single_frame_tip.js` 新增第 10 组 7 项：恢复逻辑等价实现 3 + 源码级护栏 4）；后端无改动。

### 2026-09-15（第十八轮 —— 图框数筛选维度 + 解析后自动预勾选（工作流优化））

**需求**：解析完成后默认勾选"图纸类型=标准 且 图框数=1"的记录，便于一键批量删除，剩下的数据单独分析。经方案对比（仅自动预勾选 / 仅加筛选 / 组合）选定**组合方案**。

**实现（纯前端，index.html + app.js）**：
1. **图框数筛选维度**：筛选栏新增 ☐ 单图框(=1) ☐ 多图框(>1)（`#fcFilterCheckboxes`，默认不勾=不约束）。独立于类型筛选的 **AND 维度**——可与 类型×图框数 任意组合圈选视图；无 `frameCount` 的记录（手动输入/解析失败/旧记录导入）不匹配任何图框数筛选。
2. **解析后自动预勾选**：筛选栏新增开关 ☐ ⚡ 解析后自动预勾选（标准+单框），默认开启。上传解析成功路径调 `maybeAutoPrecheck(rec)`——本次新增记录中「主导类型=标准 且 frameCount=1 且 非混合」的直接加入勾选集，配合既有批量删除按钮一步完成清理。仅作用于上传路径（手动输入 / CSV 导入 / 解析失败不触发），不动历史记录与用户已有勾选。
3. `addRecord` 改为返回新记录对象（供预勾选判定）。

**设计决策**：混合记录（多框且标准+非标并存，主导类型 standard）**不预勾选**——有分析价值应保留；勾选状态本轮**不做 localStorage 持久化**（保持改动最小）；"标准+单框"仅是默认规则，任意其他删除条件用「图框数筛选 × 类型筛选 + 表头全选」组合即可覆盖，无需改代码。

**验证**：前端 7 文件 **190 项全绿**（`test_single_frame_tip.js` 新增第 9 组 17 项：图框数匹配 7 + 预勾选判定 6 + 源码级护栏 4）；后端无改动。

### 2026-09-15（第十七轮 —— 四川自贡施工图.-19.dwg：封面被孤证复核误杀（新增互证⑥：嵌套内外框））

**现象**：四川自贡施工图.-19.dwg 含封面应解析 19 张，程序仅检出 18 张（15 张 28331×20119 + 3 张 14165×10059）。用户确认封面尺寸 **30443×20816**。

**根因**：封面是**闭合多段线双线框**（外框 30443×20816 + 内框 29243×19616，去重保留外框），非块、无标题栏条纹、全图唯一尺寸，比例 1.4625 与 18 张 SDSDFD 图框的主导比例 1.4082 不同——孤证复核的互证①~⑤（同尺寸副本 / 同比例 / 自带条纹 / 内含条纹子候选 / 块名含图框）**全部落空**，外框被当孤证剔除，内框随后也被剔。

**修法**：孤证复核新增**互证⑥：嵌套内外框**——候选内部完全包含另一个通过特征且尺寸不同的 frame_like 候选即视为有互证。双线封面/双线图框的典型结构就是"外框包内框"，而外包络（内含 ≥2 张图框的大框）仍由包裹框剔除兜底，互证⑥不会放行外包络。

**验证**：后端 43/0（新增用例 43：双线框互证场景 fc 18→19 且外框尺寸精确保留）；30 张全量 before/after 回归（HEAD 基线副本）**一致 29 / 变化 1**，唯一变化即自贡 fc 18→19（30443×20816×1 入列），其余 29 张含尺寸明细逐字节一致。

### 2026-09-14（第十六轮 —— 一层.dwg（装修图纸-18张）：无图框图纸报 401 框（frame_count 兜底链 + 前端 0 框透传））

**现象**：一层.dwg（装修图纸-18张/建筑-非标未测试，与第十三轮的一层.dwg 同名异图）实际 0 个图框，页面却显示 **401 个**。后端日志与本地直调均证实 smart 模式已正确判定"未检测到图框"——错误不在识别层。

**根因（两层叠加）**：
1. **后端兜底错位**：`frame_count = len(frame_like) if frame_like else len(all_candidates)`——frame_like 为空时兜底成**全量候选数**。该图模型空间全是家具/构件小矩形（401 个候选），smart 模式报错后前端按既有约定自动用 force_max 重试，force_max 返回的 frame_count 正是这个兜底值 401。
2. **前端把 0 当无效值**：即使后端返回 0，前端 4 处仍会兜底成 1——上传路径 `frame_count > 0` 才采纳、`addRecord` 的 `|| 1`、渲染分支 `> 0` 才显示数字（0 显示"—"）、导出同款 `|| 1`。造成气泡"共 0 个"与主界面"1 个"不同步。

**修法**：
- 后端 `app.py`：`frame_count = len(frame_like)`（无图框如实报 0）；force_max 路径 `candidates` 同步去兜底（避免"0 图框 + 401 行明细"自相矛盾）。
- 前端 `app.js`：四处改为 **0 是合法值**——上传 `frame_count >= 0` 采纳（字段缺失才按 candidates 数 → 1 兜底）；`addRecord` 用 `typeof` 判定（仅手动输入等无 framesInfo 场景默认 1）；渲染 `>= 0` 显示数字"0"（"—"只留给手动输入/失败记录）；导出 0 如实导出。

**验证**：后端 42/0；前端 7 文件 **173 项全绿**（`test_single_frame_tip.js` 新增第 8 组 11 项：0 透传/兜底链/渲染分支/导出 + 3 条源码级防回退护栏）；HTTP 实测 force_max 返回 `frame_count=0, candidates=0`，smart 报错前缀不变（前端自动重试约定不受影响）。

**运维注**：本次排查再次遇到 5000 端口旧 Flask 进程未随代码更新（debug 重载失效 + 双进程抢端口致上传连接重置），需手动结束旧进程后页面才反映新代码。

### 2026-09-14（第十三轮 —— 一层.dwg：无图框图纸误检清零（孤证复核））

**现象**：一层.dwg 实际 0 个图框（纯平面图 + 电梯井道大样标注），程序报 4 个——12240×8770 / 7280×3440 / 3290×4775（标注层 INSERT 块，大样外轮廓）+ 5160×2730（直线矩形）。

**根因**：条件C 的 rel 分母是"layout 内最大候选"。当图纸没有真图框时，最大候选本身就是标注块（12240×8770 电梯大样外轮廓），其余标注块相对它 ≥10%，全部经条件C 混入——**相对面积基准在无图框图纸里整体失真**。

**修法（孤证复核，is_frame_like 之后的反向过滤）**：真图框几乎从不"孤证"。候选满足以下全部条件才剔除：① 仅经条件C 通过（`_via=='C'`，A/B/D/E 本身是强证据，is_frame_like 现在给每个通过打 `_via` 标记）；② area_ratio < 5%；③ 无任何互证——同尺寸副本（≥2 份）/ 同比例兄弟 ±1%（≥2 份）/ 自带标题栏条纹 / **内含带条纹子候选**（罗马都市 21121×14813 图框内含 174 个子候选其中 2 个带条纹；一层大样外轮廓内含 0 条纹）/ 块名含"图框/frame"。开发中曾误伤罗马都市gai222 的单张真页框（fc 2→1），靠互证④⑤救回——单张唯一尺寸的真图框必有标题栏可依。开关 `FRAME_PARSER_NO_SOLO_RECHECK`。

**验证**：后端 42/0（用例 41 无互证全剔 fc=0、42 同尺寸副本互证保留 fc=2）；28 张全量回归（开关 A/B）逐图 diff——27 张一致，唯一变化 = 一层.dwg fc 4→0；罗马都市 fc=2 保持。

### 2026-09-14（第十二轮 —— 1号2号楼柱平法施工图：底图外框误检（1→11））

**现象**：实际 11 个图框（8×126150×178350 + 84100×59400 + 2×126150×59400），程序只报 1 个——630801×2407287 的巨型底图块 S-0-COLS（占整图面积 76%）。

**根因 1（包裹框剔除失效）**：`strip_wrapping_frames` 对"块参照插入"类型一律豁免（当年保护"单页图框块"）。但底图参照外框同样是块，豁免导致它漏剔 → 嵌套去重"留大剔小"把 9 个真图框全吃掉。**修法（强证据豁免）**：块参照仅在"内部尺寸组弱"（组数 <3 且无同尺寸组 ≥3）时豁免——单页图框块内部至多 1~2 个小尺寸组；底图外框内部必有 ≥3 个同尺寸真图框（本例 126164×178350 ×5）。

**根因 2（3 张 A0 加长框被当装饰阵列）**：8 张大框中 5 个是 `template_院标准图框_a0`（√2 比例，白名单保护 ✓），另 3 个是同模板的标注版本 `template_院标准图框_a020221031172552`——图框本体同为 841×1189@150，但块内多贴了会签栏/参照标签（图框线本身在冻结图层不可见），bbox 被撑到 126164×222938（ratio 1.767，超出 √2±10%）→ 3 个垂直排列被"对齐排列剔除装饰块"误杀。**修法（同模板短边保护）**：对齐剔除两遍扫描——先收集所有命中 A系列/√2 保护的图框块短边，其他组若短边与之一致（±1%，同模板复制必同短边）→ 一并保护。装饰块短边不会恰好等于图框短边。

**验证**：后端 40/0（新增用例 40 底图外框强证据剔除）；28 张全量回归 before/after 逐图 diff，26 张完全一致，唯二变化即两张目标图纸（1号2号楼 1→11、香槟半岛 26→27）。

> 备注：3 张标注版本图框报告尺寸为 126164×222938（含外溢标签的 bbox）而非纸面 126150×178350——图框线实体在冻结图层中不可见，块内又无图框线可推断纸面范围。数量与位置均正确，前端框略大于纸面。

### 2026-09-14（第十一轮 —— 香槟半岛：表格内容页救援（条件I））

**现象**：实际 27 个图框，检出 26。漏的是「图纸目录」页——闭合多段线外框 19945×15703（ratio 1.2703），画在图框条带下方。

**漏检根因**：该目录框不命中任何现有通道——ratio 1.2703 非 √2（条件B/D）、rel 面积仅 0.5%（条件A/C）、不是块（条件E）、无标题栏条纹且比例不匹配已入选（条件G）、不匹配主导比例 1.418（条件H）。

**修法（条件I·表格内容页救援）**：图纸目录 / 材料表 / 设计说明这类"表格页"的通用结构是外框内部存在**密集水平表格行线 + 竖向列分隔线**（实测目录框内横线 59 条 / 竖线 13 条，表格线按单元格分段绘制，故按总数计数而非要求贯穿）。判据：① 宽高 ≥5000mm（图框级）② ratio ∈ 合规区间 ③ 未被任何已入选图框完全包住 ④ 框内水平 LINE ≥30 ⑤ 竖直 LINE ≥6。

**验证**：后端 39/0（新增用例 38 表格页救援、39 被包住不救援）；28 张全量回归 before/after 逐图 diff，**27 张完全一致，唯一变化即香槟半岛 26→27**，检出尺寸清单与人工核对清单逐项一致。

> 设计取舍记录：诊断中曾尝试 ① 扩展 `_get_block_world_bbox` 支持 DIMENSION/MTEXT（会改变全部块候选尺寸口径，与既有核对表冲突，回退）；② "孪生副本救援"（同模板一份嵌在图框内、一份独立摆放则救独立份）——但独立户型详图块不是图框页，人工核对清单不含它，该信号会把非图框内容救进来，已废弃。

### 2026-09-14（第十轮 —— 两处修复：INSERT 旋转度/弧度混用 + 候选明细截断）

**修复 1：INSERT 旋转角单位错误（胜利公寓２"马桶块 bbox 错位"遗留 bug）**

- **现象**：`马桶` 块 INSERT 候选 bbox 算到 (419856,-1098934)、尺寸 900×850，真实为 (618129,-60412)、500×750。
- **根因**：DXF 的 `rotation` 组码单位是**度**，代码把度值直接喂给 `math.cos/sin`（按**弧度**解释）——rot=180 被当成 180 弧度≈233.2°，bbox 由 500×750 "转"成 900.1×849.6（500·|cos|+750·|sin| 精确吻合）、位置飞出百万 mm。更严重的隐含后果：**带旋转的图框块会因 bbox 畸变整图"未检测到图框"**。
- **修法**（app.py 三处，统一为 DXF 变换规范 `p_world = insert + R(rot)·S·(p − base)`）：
  1. 主 INSERT 候选路径：`rotation` 度→弧度换算 + 扣除块基点 `base_point`；
  2. 布局空间网格救援路径：同上；
  3. `_get_block_world_bbox` 嵌套 INSERT 组合：补上子块旋转（度→弧度）与子块基点。
- **验证**：块候选 bbox 与 ezdxf 内置精确 bbox 逐一比对 **4/4 零偏差**；全量 22 张回归 **fc 与尺寸清单逐图完全一致**（该图 27 个顶层 INSERT 中 23 个带旋转，均不影响 fc）；后端 **37/0**（新增用例 36/37：rot=90 度→弧度 / 非零 base_point 扣除——两用例在修复前代码上均"未检测到图框"式失败，有效锁定）。另：候选 payload 新增 `bbox` 字段（前端向后兼容）。

**修复 2：候选明细 20 条截断导致气泡"共 20"vs"模型空间 ×21"不一致（春风公寓２）**

- `build_payload` 早年按面积降序只返回前 20 条明细，面积最小的 10735×7432 被切掉；前端气泡标题/尺寸清单按截断明细统计（20、3 种尺寸），空间分布按 `frame_counts_by_layout` 全量统计（21）——两口径对不上。**检测未漏，纯展示层不一致。**
- 修法：去掉 `[:20]` 截断全量返回（明细必须与 frame_count 同源同量）。端到端复测：fc=21、明细 21 条、4 种尺寸齐全。

**顺手清理：气泡"前10 条摘要行"移除** —— 多图框气泡中原有的 `tip-summary` 行（原始明细前 10 条连排，如 `26000×18000(模型空间)、…`）是聚合清单上线前的旧展示，信息已被下方"按尺寸聚合"清单完全覆盖，在同尺寸多框场景（21 框同尺寸）纯属噪音 → 移除该行及其 CSS；`framesText` 仅保留为 `data-all` 缺失时的兜底数据源。前端 162 项测试全绿。

### 2026-09-14（第九轮 —— 春风公寓２：主导比例内容救援（条件H）+ 包裹框主导比例剔除）

**现象**：`春风公寓２.dwg` 实际 21 个图框，程序只检出 16 个。

**图面实况**（渲染确认）：21 个图框是**同一模板按 4 种出图比例**排版（含一组"详图小页"），长宽比全部精确等于 **1.4444**——

| 框 | 尺寸 | 数量 | 检出情况 |
| --- | --- | --- | --- |
| A 型 | 26000×18000 | 16 | 15 检出 + **1 张被内容区大框嵌套去重吃掉** |
| B 型 | 17472×12096 | 1 | **漏检**（右侧远端区域） |
| C 型 | 14560×10080 | 3 | **漏检**（右侧远端区域） |
| D 型 | 10735×7432 | 1 | **漏检**（右侧远端区域） |
| 干扰 | 67627×31514（虚线层内容区边界，ratio 2.146） | 1 | **被误当图框**，还吃掉了内部的 A 型真页框 |

**漏检根因（两个独立缺陷叠加）**：

1. **右侧 5 张小图框全通道否决**：rel 分母 = 最大的图框，5 框 rel 3.7~9.9% <10%（条件C 拒）；area_ratio 0.1~0.4% <15%（条件A 拒）；短边 7432~12096 >2000（条件B 结构性不可达）；非直线矩形（条件D 不适用）；**标题栏只有一条全高分隔线、没有横向分隔线**（条件G 条纹签名不命中——与胜利公寓２的标题栏画法不同）。
2. **内容区大框冒充图框并吃掉真页框**：`67627×31514`（虚线层，ratio 2.146）rel=100% 过条件C；包裹框剔除判据"内含 ≥2 独立尺寸组 或 单组 ≥3 份"都不满足（内部只有 1 张 A 型页框 + 其 25600×17600 内框，两尺寸归同组仅 2 份）→ 漏剔 → 嵌套去重"留大剔小"把真页框剔了。

**修法（两部分，均走"主导模板比例"这一通用信号）**：

主导模板比例 = 同 layout 内 **≥3 个已入选图框共享的长宽比（±0.5% 聚类）**。同一模板缩放出图，无论比例多少倍，长宽比不变——比例即模板指纹。

1. **条件H（主导比例内容救援）**：未入选的闭合多段线/直线矩形候选，若 ① ratio 命中主导比例（**聚类与匹配均 ±0.5%**）② bbox 内完全包含 **≥12 个实体**（真页面框内必有大量图纸内容：实测 52~839；比例恰好落在主导比例附近的窗户 decoy 1277×882 ratio 1.4478 框内只有自身边线，实测 5~6 个，被内容数干净排除）③ 已被已入选图框包住的不救（防噪音，1280×1850 虚线窗套实证）→ 救援。被救入的内框（17203/14336/10570）随后由嵌套去重收掉，不影响计数。
2. **包裹框主导比例剔除**：某内部尺寸组比例命中主导模板（±0.5%）且与外框比例差 >10%（排除同框重复画法）→ 外框是内容区边界，即使只有 1 组也剔除。

**首轮回归发现并修复的误救（新古典式 2→3）**：±1% 容差下，21728×15365（ratio 1.4141）与其均匀内缩内框 21302×14939（ratio 1.4259，绝对内缩使 ratio 偏移 0.83%）合并成伪主导簇，家具详图边框 3900×2720（ratio 1.4338，距 1.4259 仅 0.55%）被误救。把聚类与匹配容差收紧到 **±0.5%** 后，两簇各自凑不满 ≥3 → 主导不存在 → H 在该图自动失效，fc 回到 2。

**验证**：

- 全量 22 张图纸 **一致 21 / 变化 1**（唯一变化 = 目标春风公寓２：`fc 16→21`、主框 67627×31514 → 26000×18000）
- 后端 **35 通过 / 0 失败**（新增用例 33/34/35：内容丰富小框被救回 / 已入选框内不救援 / 内容区边界剔外留内）
- 前端 **162 项全绿**（40+18+31+7+33+24+9）
- `FRAME_PARSER_NO_COND_H=1 FRAME_PARSER_NO_WRAP_DOM=1` 双开关复跑**精确回到基线**，A/B 隔离有效
- 测试基准新增春风公寓２行；开关表新增 2 项

### 2026-09-14（第八轮 —— 胜利公寓２：同模板缩放套图救援（条件G））

**现象**：`胜利公寓２.dwg` 实际 6 个图框，程序只检出 4 个。

**图面实况**（渲染确认）：6 个图框是**同一模板按 4 种出图比例**排版的套图，长宽比全部精确等于 **1.4257**——

| 框 | 尺寸 | 相对大框比例 | rel | area_ratio | 判定结果 |
| --- | --- | --- | --- | --- | --- |
| 大框 ×3 | 26305×18450 | 1× | 100% | 10.52% | 条件C ✓ |
| 小框 D | 10522×7380 | 1:2.5 | 16.0% | 1.68% | 条件C ✓ |
| 小框 E | 6576×4613 | 1:4 | 6.25% | 0.66% | **全通道否决** |
| 小框 F | 5892×4133 | 1:4.46（非公度） | 5.02% | 0.53% | **全通道否决** |

**根因**：特征判定是"逐实例"的。rel 分母 = 最大的图框（26305×18450），小尺寸实例 rel 天然吃亏（E 6.25% / F 5.02% <10% 条件C 拒）；短边 4613/4133 超 2000 条件B 结构性不可达；area_ratio <15% 条件A 不可达；尺寸互不相同（同尺寸 ≥2 的条件D 也不适用）。四条路全堵 → 漏检 2 张。

**修法（条件G）**：初选（A/C/B/D/E/族传播）完成后二扫——未入选的**直线矩形**若同时满足：
1. **结构签名**：带「标题栏条纹」（框右侧/左侧 25% 区域内两条覆盖 ≥80% 框高的竖线围出带宽 ∈[5%,25%]×框宽 的竖带 + 带内横向分隔线 ≥2 条；框边界本身算候选边，条纹竖线允许两端小幅缩进——贯通反而会被内部分隔线判定拒绝整框）；
2. **同模板**：长宽比与同 layout 内某个已入选直线矩形图框一致（±1%）——同一模板不同出图比例，比例不变；

→ 救援放行。

**为什么不用"同 ratio 分组 ≥3 就放行"**：本图 `ratio 1.101 ×8`（500×550 门窗符号）、`ratio 1.333 ×4`（2000×1500 房间）等分组都会被误抓。条纹签名是硬结构证据：6 个真框全部命中（带宽稳定 ≈11%×框宽、分隔线 16 条），10 个房间/构件 decoy 全部不命中（柜/架符号也会围出窄带但带宽仅 ≈2.4%，被 5% 下限滤除）。签名在候选收集阶段预计算（仅 ratio ∈ 图框区间、面积前 400 的直线矩形），性能有护栏。

**与同块名族传播的分工**：那条管"块参照"族（同块名多次插入），本条管"直线矩形画的套图"（无块、纯线段、倍率非公度 4.46 倍）。

**验证**：
- 全量 22 张图纸 **一致 21 / 变化 1**（唯一变化 = 目标胜利公寓２：`fc 4→6`、主框不变 26305×18450）
- 后端 **32 通过 / 0 失败**（新增用例 31/32：带条纹小框被救回 / 无条纹不救援）
- 前端 **162 项全绿**（40+18+31+7+33+24+9）
- `FRAME_PARSER_NO_COND_G=1` 开关复跑**精确回到基线**（fc=4），A/B 隔离有效
- 端到端 HTTP 复验：`frame_count: 6`、`{"模型空间": 6}`、`26305 × 18450`
- 测试基准新增胜利公寓２行；开关表新增 `FRAME_PARSER_NO_COND_G=1`

### 2026-09-11（第七轮 —— DS4 四层阁楼平面系统图：布局空间网格图框救援 + 布局空间优先）

- **现象**：`DS4 四层 阁楼 平面系统图 (1).dwg` 实际 6 个图框，程序只报 5 个
- **根因（与前六轮都不同：不是判定漏检，而是"读错了空间"）**：该图纸有 3 个布局 —— `Model`(2008 实体)、`布局1`(521 实体)、`布局2`(空)
  | 空间 | 有图框吗 | 程序检出 |
  | --- | --- | --- |
  | 模型空间 | ❌ 没有（1:1 实际尺寸的图纸内容） | 5 个（**全是误判**） |
  | **布局1** | ✅ **6 个等大页面框** | **0 个（全被预筛剔除）** |
  - 6 个真图框 = 块 `A$C5AB964F6` 的 6 个实例，尺寸全等 **434.1 × 311.1**（ratio 1.3953），位置严丝合缝排成 **3 列 × 2 行**：X = `2104.0 / 2538.1 / 2972.2`（列距 434.1）、Y = `-494.2 / -183.1`（行距 311.1）
  - **为什么全被剔除**：INSERT 预筛要求"短边 ≥500 **或**命中 A 系列短边"。`311.1 < 500` 且 `311.1 ∉ {841,594,420,297,210,148,105}` → 6 个全被当"家具符号块"剔除。日志实证：`[布局 "布局1"] INSERT 块参照: 共 25 个 | 预筛剔除 8 个 | 入库 0 个`
  - 关键：**311.1 = 297 × 1.0475** —— 这是一张按 1.0475 倍画在布局空间的 A4 框。布局空间通常 1:1，此图做了整体缩放，于是"标准尺寸整数判定"失效
  - 模型空间那 5 个（`9900×12900`、`13423×9014`、`8926×13464`、`8378×13316`、`4870×6650`）是 1:1 的**内容区/闭合轮廓**，图上根本没有图框 —— 它们通过条件 C（rel 94.74%/94.11%/93.43%/87.36%）与 A，属于"巨型内容块冒充页面框"
- **被否决的前一版方案（S3 / 条件 F，已回退）**：曾按"6 个整页图纸块、其中一个被 ratio 死区挡住"的错误假设，新增条件 F（`rel ≥50%` + `ratio ∈ [√2×0.88, √2×1.10]` + 尺寸整数）放行 `9900×12900`。实装后 fc 仍为 5 —— 因为该候选与 `4123214232 8904×13401` 在去重阶段 IoU 0.82 被判重复，留面积大的（新的）顶掉旧的。**用户随后给出截图纠正**：预期是 6 个**等大**图框，不是"6 个尺寸各异的整页块" —— 该假设被推翻，条件 F 全量回退（`app.py` 中相关常量与判定块已删除）
- **修法A（本轮实装，两部分）**：
  1. **布局空间网格图框救援**（`collect_candidates_from_layout` 内，INSERT 预筛的补充通道）：仅对**图纸空间布局**生效。判据 = 同块名同尺寸（宽高各 ±2mm）INSERT **≥4 个** + 长宽比 ∈ 图框区间 + **排列成网格**（按 X/Y 一维聚类得列数 ≥2、行数 ≥2，网格覆盖率 ≥80%，排除"一列排开"）。命中即豁免短边下限入库，**是否算图框仍由 `is_frame_like` 决定**。DS4 实测：`6 个 434.1×311.1 排列成 3 列 × 2 行（覆盖率 100%）→ 豁免短边预筛`
  2. **布局空间优先清洗**（候选清洗第 5 条）：布局空间已识别 **≥4 个**图框且模型空间也有候选 → 剔除**模型空间的全部候选**。理由：布局空间是出图页面空间，一旦排出多个等大页面框，模型空间就只是内容源，其内任何大框都不是图框。DS4 实测：`[布局空间优先] 布局空间 ['布局 "布局1"'] 已识别 6 个网格排版图纸页 → 剔除模型空间候选 5 个`
- **为什么第 1 部分要限定"仅图纸空间"**：本地 21 张校验图纸里 **19 张只有模型空间**（非模型布局全空或仅含视口），布局空间是稀缺信号 —— 用它做救援的误伤面天然很小
- **为什么第 2 部分门槛取 ≥4 而不是 ≥2**：`一层平面图3.25_t3.dwg` 布局1 有 12 个 A1 套图小页框（`841×594`），本就靠条件 C 正常识别、且模型空间无候选，不受影响；`后桥半轴总成A1-840x593.dwg` 的 `Gb A1 标题栏` 布局 1 个框也正常。≥4 的门槛只在"成规模排版"时才触发模型空间清理
- **验证**：全量 21 张图纸 **一致 20 / 变化 1**（唯一变化 = 目标 DS4：`fc 5→6`、归属 `模型空间:5`→`布局1:6`、主框 `13423×9014`→`434×311`）；重点非模型布局图纸 `一层平面图3.25`(11/布局1)、`后桥半轴总成`(1/Gb A1 标题栏) **完全不变**；后端 **30 通过 / 0 失败**；前端 **162 项全绿**；`FRAME_PARSER_NO_GRID_RESCUE=1 FRAME_PARSER_NO_LAYOUT_WINS=1` 双开关复跑精确回到基线（fc=5 / 模型空间:5）
- **开关**：`FRAME_PARSER_NO_GRID_RESCUE=1`（关救援）、`FRAME_PARSER_NO_LAYOUT_WINS=1`（关布局空间优先）

### 2026-09-11（第六轮 —— 两项"既有失败"用例的收尾：改用例，不改代码）

- **背景**：`test_backend_rules.py` 长期有 2 项失败，与任何一次功能改动都无关（用 `FRAME_PARSER_NO_DENOM_GUARD` 开关双跑确认过）。本轮单独收尾，**结论是两项都属用例本身有问题，`app.py` 一行未动**
- **用例 15（原 `每 layout 帧数上限`）→ 改名 `同 layout 多尺寸标准幅面: 入选数受 rel 门槛约束`**
  - 原断言 `frame_count == 2`，实测 4。原注释称"A3/A4/A5 被条件 B 的短边下限过滤"——**只有 A5 落选，且真因不是短边**
  - 逐框实测（rel 分母 = layout 最大候选 A1 = 499554，layout 总 bbox = 1669140）：
    | 框 | 尺寸 | area_ratio | rel | 命中通道 |
    | --- | --- | --- | --- | --- |
    | A1 | 841×594 | 29.93% | 100% | **条件A**（占比 ≥15%） |
    | A2 | 594×420 | 14.95%（差 0.05% 够不到 15%） | 49.94% | **条件B**（短边 420 ∈[400,2000]） |
    | A3 | 420×297 | 7.47% | 24.97% | **条件C**（短边 297 <400 过不了B） |
    | A4 | 297×210 | 3.74% | 12.49% | **条件C** |
    | A5 | 210×148 | 1.86% | **6.22% < 10%** | **三通道全挂 → 落选** |
  - A5 真正死于 **rel 门槛**（<10%），不是短边。A3/A4 由条件 C 放行是设计行为（rel 法与绝对尺寸无关）
  - 修正：断言 2→4，注释按实测重写。**不删除**——修正后它锁住了"同一批标准幅面里最大的几个入选、最小的被 rel 门槛拒"这条有价值的行为
- **用例 22（`条件B尺寸规整: 非整数闭合多段线被过滤`）→ 修 fixture 的 ratio，不动判定逻辑**
  - **放行路径查证**：候选 `1895.28 × 2629.36`（ratio 1.3873、area_ratio 0.0623%）实际走的是**条件 C**——它是该 layout 最大候选，rel=100%，而 **C 通道根本不检查尺寸规整**。条件 B 的尺寸规整确实拒绝了它，但判定顺序上 C 在 B 之前且已放行
  - **关键设计点（本轮最大发现）**：条件 B 的"尺寸规整"实际**可达区间极窄**。C 在 B 之前判定，两者 ratio 要求重叠：
    - C 通道 ratio 下限：非直线矩形 `√2×0.95 ≈ 1.344`、直线矩形 `1.10`
    - B 通道 ratio 区间：`√2±10%` 即 `[1.273, 1.556]`
    - → 若 ratio ∈ `[1.344, 1.556]`，**C 必然先放行**，B 永远走不到；只有 ratio ∈ `[1.273, 1.344)`（**宽度仅 0.071**）时 C 被挡住、B 可达
    - → **直线矩形因 C 下限 1.10 < B 下限 1.273，永远走不到 B**；"尺寸规整"目前只对非直线矩形（闭合多段线/块参照/降级包围盒）可达
  - 修正：把 fixture 的 ratio 从 1.387（落进 C 的放行区）改为 **1.30**（落进 B 的可达区），尺寸保持非整数 `1895.28 × 2463.86`；用例 23 同步改为整数 `1895 × 2464`（同 ratio 区间）。两例**唯一变量是"尺寸是否取整"**，结果一拒一收 —— 这才真正证明了"尺寸规整"是决定性判据
- **被验证否决的修法（记录备查）**：曾考虑给条件 C 补"尺寸规整"约束，**实测否决**——
  - 38 张校验图纸扫描：**真图框普遍带小数尾数**且与内容轮廓区间**完全重叠**：2.8米皮带线 `2970 × 4200.43`（h 偏差 0.43）、一楼大厅 `57775.59 × 40851.43`（0.41/0.43）、长中苑 `28261.03 × 19984.59`（0.41）、55555555 `25904.24 × 22068.88`（0.24），而用例 22 的"内容轮廓"偏差是 0.28/0.36 —— **真图框的尾数比内容轮廓还大**。容差 0.1 太严（会误伤前四个），放宽到 0.5 又挡不住用例 22
  - 换成"能否归因到标准幅面 × 常用比例 k"：用例 22 失配 1.92%，比一楼大厅（0.01%）、长中苑（0.01%）都差（方向对），但 55555555（18.58%）和运煤胶带机（31.37%）这两个**真图框**又远超它 —— 仍无法区分
  - 结论：条件 C 的"不看尺寸"是**有意设计**（相对面积法本来就与单位/绝对尺寸无关），真图框带缩放尾数是普遍现象，给它加尺寸约束会系统性削弱其存在意义
- **验证**：后端 **27 通过 / 0 失败**（两项既有失败全部转绿）；前端 **162 项全绿**；关键图纸端到端复验全部一致（运煤胶带机 1、55555555 5、世欧澜山 15、长中苑 17、2.8米皮带线 13、一层平面图 11）

### 2026-09-11（第五轮 —— 运煤胶带机.dwg 图框过检：rel 分母塌缩防护）

- **现象**：`运煤胶带机.dwg` 实际只有 **1 个图框**（整张图的外边界），程序却检出 **7 个**
- **图纸性质**：**1:1 设备布置图 / 总图**——全图 `149823×77215`（约 227m×77m，图内文字实测含「胶带输送机名称及规格」「227000.013」，是 227m 长皮带线的设备平面布置）就是唯一图框，图内**没有纸框、没有标题栏**，其余 653 个候选全是设备构件矩形（托辊、支腿、料斗等）
- **根因（rel 分母塌缩）**：
  1. 整图外轮廓 `149823×77215`，长宽比 **1.9403**、占 layout 面积 **97.6%** —— 命中「rel 分母排除判据①」（占比 ≥50% 且长宽比偏离 √2 超 10%），被**踢出 rel 分母**
  2. 分母随即跌落到 layout 内最大的设备构件 `8000×7500`（长宽比 1.067 近方形，能过所有排除判据），它**只占 layout 面积的 0.51%**
  3. 于是所有候选的 rel 被整体放大 **192.8 倍**：7 个设备构件（`4054×3159` ratio 1.283、`4000×2000` ratio 2.0、`4000×1600` ratio 2.5、`4011×1777` ratio 2.257、`1002×660` ratio 1.518×2）的真实相对面积只有 **0.06%~0.11%**，虚报后变成 **10.7%~21.3%**，全部越过条件 C 的 10% 门槛
  4. 这 7 个候选**互不包含**（矩形两两不相交），散布在全图各处（沿皮带线分布的构件），因此也逃过了包裹框剔除
- **为什么不能简单加"分母下限"**：试过固定下限（分母 ≥ layout 面积 × 0.25），**破坏 7 张图纸**（长中苑 17→1、滨江 25→1、世欧澜山解析报错等）——真图框的分母占比可低至 **0.11%**，与塌缩分母的 0.51% **区间完全重叠**；长宽比信号同样重叠（一层平面图正确分母 1.029 比运煤胶带机的 1.067 还差）。**任一单一信号都不可用，必须组合**
- **最终判据（组合信号）**：`分母占 layout 面积 < 1%` **且** `存在占 layout 面积 ≥50% 的被排除大框` → 判定为**「整图即图框、内无图框级参照系」**，该空间的 rel 一律置 0，只保留条件 A / 条件 B 通道（不靠相对面积判定）
  - 逻辑自洽性：rel 的比较基准已经缺失（最大候选都被排除了），此时相对面积无意义；而"存在占半图以上的被排除大框"正说明这个空间的图框就是那个大框本身，图内构件不应参与竞争
  - 真实图纸页框场景：被排除大框要么不存在，要么分母本身就是真页框（占比远超 1%），两个条件不会同时成立
- **修复结果**：整图外轮廓改由**条件 A（占比 97.6% ≥ 15%）**选中 → `frame_count = 1`、`149823×77215` ✅，日志打印明确判定说明（`[模型空间] rel 分母（59,999,989）仅占 layout 0.51%，且存在占 layout 97.6% 的被排除大框（149823×77215 ratio 1.940）→ 判定为『整图即图框、内无图框级参照系』...`）
- **A/B 回归对照**：新增环境变量 `FRAME_PARSER_NO_DENOM_GUARD=1` 可关闭该防护（**仅供回归脚本使用**，生产不设此变量）。21 张校验图纸全量双版本串行比对：**一致 18 个 / 变化 1 个**，唯一变化就是运煤胶带机（7→1）；`2.8米皮带线 13`、`长中苑 17`、`世欧澜山 15`、`一层平面图 11`、`一楼大厅 10`、`滨江 25`、`55555555 5`、`RF雅安 9`、`电子称 10`、`02食堂 5` 等**全部保持原值**
- **测试**：`test_backend_rules.py` 新增**用例 25**（整图外轮廓被排除 + 分母塌缩到近方形场地符号 + 3 个设备构件实测 ratio 2.0000/2.5000/2.2571），防护关闭时 `frame_count=3` FAIL、开启时 `1` PASS，A/B 区分有效；后端合计 **25 通过 / 2 失败**（该 2 项为**既有失败**：`每 layout 帧数上限`、`条件B尺寸规整: 非整数闭合多段线被过滤`，改动前后一致，与本轮无关）

### 2026-09-11

- **修复气泡打不开（重复 `class` 属性）**：`tipAttrs` 模板串里带了 `class="tip-anchor"`，与外层 `<span class="...">` 拼接后同一标签出现**两个 `class` 属性**。按 HTML 规范的 duplicate-attribute 规则浏览器取后者，第一个 `class` 里的样式类被整个丢弃（`html.parser` 实测：`class="type-tag multi-frame tip-anchor" class="tip-anchor"` → 最终只剩 `tip-anchor`），徽章样式丢失且标签属性结构异常。多图框、单图框**都受影响**。现改为 `tipAttrs` 只含 `data-*` 属性，`class` 由各分支自行拼装一次
- **气泡交互统一为点击**：去掉 `mouseenter` 悬停弹出与 `mouseleave` 延时隐藏（`tipHideTimer`/`TIP_KEEP_MS` 一并清理）——悬停在表格里容易误触、闪动，点击更可控。现在：点锚点弹出 → 再点同一锚点 / 点页面别处 / 点气泡内「✕ 关闭」收起；单图框与多图框走完全同一套交互。底部按钮由「📌 固定」改为「✕ 关闭」（点击模式下不再需要固定）
- **新增端到端 DOM 测试** `test_tip_interaction.js`：用 jsdom 真实渲染 DOM，跑通「锚点可选到 → 点击打开 → 再点关闭 → 点别处关闭 → ✕ 关闭 → 无数据降级显示 `—`」共 18 项；未装 jsdom 时自动 SKIP 不阻塞。`test_single_frame_tip.js` 同步扩充到 40 项，新增源码级回归断言（禁止 `tipAttrs` 含 `class=`、禁止重复 `class` 拼接、禁止残留悬停代码）
- **单图框气泡展示空间来源**：此前空间布局气泡只在多图框（`frameCount > 1`）时出现，单张图纸看不到图框来自模型空间还是布局空间。现改为**所有记录**的「图框数」列都是气泡锚点——单图框时气泡内以「📐 空间来源」单行展示所在空间（如 `模型空间`、`布局 "Sheet1"`），多图框仍走「📐 空间分布（N 个空间）」逐行清单；图框数列本身仍只显示数字（单框加虚线下划线提示可点击），不占额外列宽
- **数据构建调整**：`framesText` / `framesAll` 从"仅多图框构建"改为统一构建（单框即 `841×594(模型空间)` 单条），新增 `primaryLayout` 承载单框空间来源；`showTip` 移除 `nRaw === 0` 早退（否则手册输入/无候选记录根本弹不出气泡），尺寸列表为空时跳过该区块
- **旧记录升级兼容**：`loadRecords` 增加字段补全——历史记录（localStorage）缺 `framesText`/`framesAll`/`primaryLayout`/`layoutCounts` 时按 `framesMeta` 就地补齐，无需重新解析图纸；`framesMeta` 也缺失的（手动输入/解析失败）保持缺失，气泡内显示 `—` 占位，绝不猜测

**同日补充（第三轮）—— Excel 导出列宽自适应**：

- **背景**：原「导出」实际是 CSV，而 CSV 格式**根本不存储列宽**，Excel 打开只能套默认宽度（8.43 字符），无法自适应；导出表里「图纸名称」经常被压得看不见
- **改导出真正的 .xlsx**：引入 **xlsx-js-style**（`vendor/xlsx-style.bundle.js`，SheetJS 0.18.5 的带样式分支，**本地引入不依赖 CDN**，离线可用）。按每列内容计算列宽后写入 `!cols`
- **列宽算法**（`computeColWidths`）：只按列内容算，与列名无关；多行单元格（图框尺寸 / 布局分布用 `\n` 分行）按**最长那一行**算而非累加；表头参与比较；结果夹在 **[6, 42]** 之间——太窄会顶出 `###`，太宽一屏放不下，故超长图名截到 42
- **字符宽度必须查表，不能"中文=2、其余=1"**：Excel 列宽单位是"默认字体（Calibri 11）下 **0 字符**的宽度"，约 **7px**；汉字/全角 ≈14px 占 2 个单位，但 ASCII 里空格仅 3.7px、直引号 `"` 与连字符 `-` 仅 4.4px，**都不足 1 个单位**，而 `M`/`W`/`m`/`w` 有 9~10px 超过 1 个单位。旧公式把窄字符当 1 个整单位，导致 `布局 "A1-出图窗口"×2` 这类**含空格+引号+连字符**的文本被高估（旧算 20，实测应 18.41）。现建 `XLSX_NARROW_PX`/`XLSX_WIDE_PX` 实测字宽表 + `displayWidth(ch)` 逐个字符累加，基准 `XLSX_MDW=7.0`，边距 `XLSX_COL_WIDTH_PAD` 由 2.5 收到 1.6（字符宽现在算得准，不再需要大冗余）
- **样式必须用支持写样式的库**：免费版 SheetJS（`xlsx.full.min.js`）**不支持写单元格样式**，`cell.s` 会被**静默丢弃**——实测导出的 zip 里 `s=` 属性根本没写入、`styles.xml` 里也搜不到 `wrapText`。后果是含 `\n` 的多行文本（图框尺寸、布局分布）在 Excel 里**被挤成一行显示**，视觉上就像"列宽没自适应"。换 `xlsx-js-style` 后 `cell.s` 才真正落盘：表头加粗 + 浅灰底（`F0F3F8`）+ 居中、数据 `vertical:'top'` + `wrapText:true`、冻结表头行（`!freeze`）
- **MIME 坑**：SheetJS 的 `XLSX.writeFile` 在浏览器分支把 Blob 的 type **写死成 `application/octet-stream`**，传 `mimeType` 参数无效（读源码确认）。改为自己 `XLSX.write({bookType:'xlsx', type:'array'})` 取字节 + 造标准 MIME 的 Blob 下载，并延时 `revokeObjectURL` 释放
- **静态托管放行 `vendor/`**：`app.py` 的静态路由原本是 `{'app.js','style.css'}` 白名单，需放行 `vendor/` 一层，并拒绝 `..` 防目录穿越（实测 `/vendor/../app.py` → 404）
- **按钮调整**：`⬇ 导出 Excel`（筛选结果，主按钮）/ `📊 导出全部 Excel` / `📄 导出 CSV`（保留，供导入流程与非 Excel 场景）
- **测试**：新增 `test_xlsx_export.js`（33 项：字符宽度查表、列宽取最大、多行取最长行、上下限夹取，并**真生成 .xlsx 再读回**校验列宽与 `customwidth=1`）；新增 `test_xlsx_e2e.js`（24 项：jsdom 加载真实 index.html + 库 + app.js，点按钮导出，读回验证 14 列列宽、中文无损、多行保留、样式索引存在）；新增 `test_layout_col_width.js`（7 项专项：空内容 / 短布局 / 长布局 / 宽度随内容变化 / 文件内实际列宽）。前端合计 **162 项全绿**

**同日补充（第四轮）——「布局分布」列宽问题定位**：

- **现象**：用户反馈「布局分布这个字段没有自适应」
- **排查结论一（算法虚高）**：列宽算法本身在跑、列宽确实随内容变化（短内容 11.6 → 长内容 34.67，已落盘到文件），但**宽度估算公式过粗**——「布局分布」的典型内容 `布局 "A1-出图窗口"×2` 夹带大量空格、直引号和连字符，这些字符在 Calibri 11 下只有 3.7~4.4px（不到 1 个列宽单位），旧公式按 1 个整单位算属于系统性高估。已按上文改为逐字符查表（`displayWidth`）
- **排查结论二（样式被丢弃，视觉假象）**：更关键的是**多行文本没有 `wrapText`**。旧库 SheetJS 免费版静默丢弃 `cell.s`，「布局分布」单元格里 `\n` 分隔的多行内容在 Excel 里被压成一行显示，超出列宽的部分直接看不见——用户看到的"没自适应"其实是**没换行**。换 `xlsx-js-style` 后 `wrapText` 生效，多行正常折行
- **验证**：实测同一份导出文件里「布局分布」列宽 **34.67**（对比短内容场景仅 11.6），列宽随内容自适应生效；生成样例 `图框检测记录_列宽自适应样例.xlsx` 校验：含 `wrapText: true` ✅、含加粗 ✅、含灰底 `F0F3F8` ✅、单元格带样式索引 ✅、`cols` 定义正确 ✅


### 2026-09-10

- **日志按大小轮转**：`basicConfig` 换成 `RotatingFileHandler`（单文件 10MB、保留 4 份历史 `parser.log.1~.4`、磁盘上限 ≈50MB、UTF-8、`delay=True`）；新增 `_archive_oversized_log()` 启动预归档（超阈值的既有日志先归档为 `.1` 并后移历史序号，被占用时静默跳过）；logger `propagate=False` 避免经 root handler 重复输出，handler 挂载做幂等处理（兼容 Flask reloader 重启与模块重复导入）。背景：`parser.log` 自 2026-09-01 累积 6.6MB 且从未轮转，一次 20 张图纸的批量回归就能写掉数 MB
- **条件E（块模板标准幅面 / A+ 通道）**：新增"块定义本身是标准图幅 + 实例按常用制图比例等比缩放"判据。2.8米皮带线.DWG 中同一个 A4 图框块（块定义 297×210）被以 ×1/×2×8/×4/×10/×20 五种比例重复插入 12 次，插入尺寸 297×210 ~ 5940×4200（跨度 20 倍）——逐实例的相对面积法必败（最大那张 rel=100%，最小那张只剩 0.25%，10 个实例全被 10% 门槛拒）。新判据不看面积占比与绝对尺寸，只看"块模板是标准幅面 + 缩放倍率 k 在白名单内"，12 个实例全部放行
- **同块名族"一荣俱荣"传播**：按块名分组，族内有任一实例通过既有判定即整族放行，约束族内所有实例长宽比均接近 √2（±10%）以防装饰块误传播。与条件E 互补：条件E 兜底"块模板是标准幅面"（即使整族都是小尺度），本传播兜底"块模板非标准但族内大尺寸实例已通过"的自定义图框块
- **回归验证**：20 张校验图纸全量对比（改动前后双版本串行解析），**仅 2.8米皮带线.DWG 变化 3→13**，其余 19 张 frame_count 与主框尺寸完全一致（世欧澜山 15、一层平面图 11、电子称套图 10、一楼大厅 10、滨江 25、长中苑 17、55555555 5、图纸目录 1、夹具装配图 6、支座 1 等均无变化）；关掉条件E 单跑本图仍为 13，证明同块名族传播路径亦可独立生效

### 2026-09-08

- **条件C rel 分母新增第二跳过判据**：面积占比 ≥15% 且长宽比 >2.5 的候选不作相对面积分母（滨江江南铭庭 PM：532588×132430 超宽内容排版外框 ratio 4.02、area_ratio 23.7%，底框外大量内容拉大 layout 总面积逃过 50% 门槛，25 张 √2 页框 rel 被压到 0.16~0.64% 全拒 → fc=1）；真图纸页框长宽比必 ≤~2.1，2.5 留足余量。02食堂顺带 1→5（良性）
- **标准判定收紧（容差 5→2mm + k=1 完全精确）**：`matchScaledStandard` 的绝对容差 TOL_ABS 5→2mm，且循环中跳过 k=1——1:1 标准幅面必须与精确表逐位相等（`matchStandardExact`），不再做尺寸误差判断（夹具装配图 420×597≈A2 差 3mm、421×298≈A3 差 1mm 等"看着像标准但尺寸不取整"的模板框一律落回非标族）；842×2084（差 4mm）随之改判 A2+3/4×2 缩放。测试用例同步更新，31/31 通过
- **布局空间分开统计图框数**：后端新增 `frame_counts_by_layout`（按 candidate.layout 分组，smart/force_max 两分支均输出，/upload 透传）；前端图框数下方以次级小字常显「模型空间×N、布局 "Sheet1"×M」，字段缺失时按 candidates 自行聚合兜底
- **导出 CSV 新增「布局分布/图框尺寸」列**：列序调整为 序号/图纸名称/图框数/布局分布/图框尺寸/宽/高/…；图框尺寸按尺寸聚合逐行 ×n（与气泡同款），布局分布每空间一行；导入改用引号感知 `parseCsvRows`（兼容字段内逗号与换行）并按表头名定位列，新旧列序均可导入
- **气泡弹窗 UI 重构**：多布局时空间分布不再单行省略截断——新增「📐 空间分布（N个空间）」区块逐行完整展示（超过 220px 可滚动）；气泡整体可交互（pointer-events 开启，列表滚动/悬停不消失，点击内部不关闭钉住），宽度 420→560px，尺寸列表 max-height 420→300px

### 2026-09-07

- **偏差百分比智能小数位**：近似匹配 detail 的偏差从固定 1 位改为智能精度（≥0.01% 显示 2 位、更小显示 3 位），消除空洞的 "0.0%"/"0.00%"（如封面 28261×19985 偏差仅 ~0.006%）
- **等比缩放判定去比例偏差 + 制图比例白名单**：`matchScaledStandard` 不再比较宽高比（√2 相似模板/半整数会擦边误判标准，长中苑 16 张模板框曾误判 A2/A4 缩放 ~39.05x），改为验证"两条边同属一个 k × 标准幅面宽高"（k 须落在 `COMMON_PLOT_SCALES` 白名单：1/2/2.5/4/5/10/20/25/40/50/75/80/100/150/200/250/300/400/500），每边绝对容差 ≤5mm；保留 2.5/75 等合法非整数比例，挡住 39.05/95.16 等自定义倍数；同形双解优先更小 k
- **混合标识两档化**：`mixedLevel` 返回 weak/strong 两档——多图框且全部标准 = weak（弱化 🌀，仅提示多张标准页）；标准+任意非标 / 无标准但多种非标并存 = strong（强化 ⚠️，醒目）。单图框或同类型多图框不标混合；徽标与筛选维度独立于主导类型（`test_judge_logic.js` 同步更新长中苑/842×2084 用例）

**同日补充（图框漏检修复）**：
- **条件C 对直线矩形单独放宽比例下限至 1.10**：新增 `LINE_RECT_C_RATIO_MIN = 1.10`，仅"直线矩形"（P1 严格矩形，四边各有完整 LINE 覆盖）走 1.10 下限，闭合多段线/块/降级路径维持 √2×0.95≈1.343——非等比拉伸的大页面框（55555555.dwg 上部两张 25904×22069 直线矩形 ratio 1.174，曾被 1.343 下限漏 2 张）召回，fc 3→5；闭合多段线不放宽（世欧澜山 45641×55212 ratio 1.21 曾作最大候选包裹剔除 2 张真页框，fc 15→14，已避坑）。全 18 个校验文件仅 5555 变化
- **条件C 的 rel 分母排除内容级底图大框**：面积占比 ≥50% 且长宽比偏离 √2 超 10% 的候选不作相对面积分母（一楼大厅及展厅.dwg：261665×214254 闭合多段线 ratio 1.22、area_ratio 92% 包住全部页面，若留作分母则 10 张真页框块 rel 被稀释到 1.3~4.2% <10% 全拒 → fc=1）；修复后 10 张页框块正常进入候选、大底框由包裹剔除收掉，fc 1→10。真整页图框即使 area_ratio≈100% 也近 √2 仍作分母无害

### 2026-09-04

- **实体坐标 OCS→WCS 统一转换**：`get_polyline_vertices` / LINE 端点经 `ocs().to_wcs()`，兼容带倾斜 extrusion 的实体
- **全局旋转矫正（UCS 图纸）**：长度加权直方图估计主方向 + 主体共旋 ≥65% 门限（防混合方向图纸误转），把倾斜图框"转正"后识别（如 A3 297×420 图框不再被矩形度门槛误杀）
- **直线矩形长边全配对**：密集聚类（>16）时对跨度 ≥300 的图框级长边聚类两两全配对 + 相邻配对双通道，线对按间距降序避免被组合上限截断（机械图纸多图框漏检修复）
- **前端记录表加宽 + 批量操作**：整体宽度拉大、列不换行信息一眼看全；新增全选/行勾选与批量删除
- **图框数可截图气泡**：自绘固定气泡（原生 title 无法截图）；完整列出所有图框（超 20 可滚动）、同尺寸聚合为 `宽×高 ×n`、一键复制全部（交互后续改为点击开合，见 2026-09-11）

**同日补充（第二轮）**：
- **直线矩形检测配对重构**：线对预过滤（公共覆盖过短丢弃）+ 按公共覆盖长度排序 + 宽高比例剪枝与右界截断、预算提至 50 万——密集图纸海量"超远装饰线对"不再抢占预算，LINE 双层封面框（28261×19985）可召回；长边配对阈值 300→180，A4/A3 及 280×195 等短边<300 的 LINE 图框不再漏检
- **特征/包裹规则加固**：直线矩形额外要求相对面积 ≥10%（CAD 远处残留矩形过滤）；area_ratio 异常过滤加 0.1% 浮点容差（图框即 layout 最大外框时不再误杀）；包裹剔除单尺寸组实例 ≥3 判拼版外框
- **全实体包围盒降级修正**：降级只统计绘图几何实体、排除 TEXT/MTEXT/DIMENSION 等（文字外溢不再撑宽图框）；布局空间已识别真图框时剔除模型空间降级框（机械图"模型空间画内容、布局放图框"结构）
- **DWG 转换 ODA audit 兜底**：ODA 默认转换产物截断（缺 ENDSEC）的 DWG 自动以 audit=True 重试
- **INSERT 预筛 A 系放行**：短边命中标准 A 系列（210/297/420…）的小块图框可入库（A4 块图框不再被家具过滤误杀）
- **前端"混合"分类**：逐框分类统计，标准族与非标族并存的文件加 🌀混合 徽标（悬浮显示构成），筛选栏新增"混合"维度，不破坏现有 4 类

**同日补充（第三轮）**：
- **LINE 矩形组合扫描坐标包含遍历**：竖对按 x 起点落在横线公共覆盖段内升序扫描（帧左边界对排最前），替代"gap 全窗口"遍历——数万线对的高密度图纸不再因预算耗尽漏检帧（电子称皮带输送系统 6→10，图层 0 LINE 页框 21475×30372×1 + 29788×21062×3 + 14894×10531×2 全部召回）；候选上限提至 5000
- **条件C 长宽比 ≤2.5 上限**：剔除内容块 bbox 冒充图框（A$C67EE090B 127686×34157 ratio 3.74 靠 rel 80% 误收）；真图纸页框比例通常 ≤2.1
- **条件D（同尺寸重复页框通道）**：同 layout 同尺寸直线矩形 ≥2 份 + √2±10% + 短边 ≥500 + rel ≥1% 即收——多尺度套图中小页框面积远小于最大图框（rel 2.9%）仍可识别；rel≥1% 挡掉成批重复的表格/阵列矩形

**同日补充（第四轮）**：
- **包裹剔除豁免 √2 纸张边框**：自身长宽比接近 √2±10% 的显式矩形（真图纸边框，如 PUB_TITLE 双层框）不再被当"外包络"剔除——单页图框内含"目录表外框 + 标题栏块"两个尺寸组时旧规则会误删真框（图纸目录 2→1）；内部内容由嵌套去重收掉，拼版外框真目标（ratio 2.6~2.7）不受影响

**同日补充（第五轮）**：
- **条件B 相对面积门槛覆盖闭合多段线**：rel≥10% 从"仅直线矩形"扩展到全部显式类型——闭合多段线的历史豁免（澜山 730×540 装饰框给包裹识别当尺寸组证据）已由"单尺寸组实例≥3"规则替代；主图框区域外的孤立小闭合矩形（rel 0.3%）不再误收（新古典式 4→2）

**同日补充（第六轮）**：
- **对齐剔除白名单扩展（√2 图框块保护）**：同块名 ≥3 同排并排的自定义尺寸图框块此前只被标准 A 系列白名单覆盖、被当装饰阵列误剔——白名单新增"长宽比接近 √2±10% 即保护"（纸张框比例标签）；装饰块（柱/门窗/家具）比例远离 √2 不受影响
- **条件C 长宽比下限放宽**：√2×0.99（≈1.400）→ √2×0.95（≈1.343）——图框块被非等比缩放插入时真实 bbox 比例会偏离 √2（如缩放后 ratio 1.388，偏差仅 1.9%），1.343 仍过滤近正方形误判（1.06/1.24）
- **去重阶段日志**：deduplicate 增加 `[去重剔除]` 记录（候选尺寸/图层与被保留大框 bbox），与包裹/对齐日志同风格

### 2026-09-03

- **包裹框识别 + 条件C 相对面积法 + 可见性过滤**：外层大框内含 ≥2 个独立尺寸图框候选判为外包络剔除；相对面积法适配多图框同 layout 与非标单位；冻结图层/实体级 invisible 过滤
- **INSERT 块参照路径 + 对齐排列剔除**：块参照插入是图框常见画法，补全识别路径；同块名对齐排列实例判为装饰块剔除
- **XREF 检测警告（方案B）**：只警告不加载，等遇到真实 XREF 图纸再升级
- **标准 A 系列白名单 + area_ratio 异常过滤**：对齐排列的标准 A 系列图框不误剔；面积超过 layout 总面积的候选必为伪候选
- **长宽比 √2 约束**：条件C ≥√2（允许加长版）过滤接近正方形的误判候选；条件B ±10% 过滤小矩形
- **空布局视口过滤**：降级路径排除 VIEWPORT，仅含视口的空布局不产生伪图框

### 2026-08-31 及以前

- 多矩形直线检测（替代全局最外框）、归一化长宽比、同源面积占比分母、safe_log 日志体系、上传上限 200MB 等基础能力

---

## 四、API

`POST /upload`（multipart/form-data）

| 参数 | 说明 |
| --- | --- |
| file | .dxf / .dwg 文件 |
| priority | polyline（默认）/ line_rect |
| unit | mm（默认）/ inch |
| mode | smart（默认）/ force_max |

返回：`{ width, height, unit, frame_count, frame_counts_by_layout, candidates[], xref_warnings[] }`

---

## 五、本地运行

```
pip install -r requirements.txt
python app.py
```

启动后访问 `http://127.0.0.1:5000`。`.dwg` 解析依赖 [ODA File Converter](https://www.opendesign.com/guestfiles/oda_file_converter)，需本机安装并保持默认路径；`.dxf` 可直接解析。

**端口配置与冲突防护**（2026-09-15）：

- 端口默认 5000，可用环境变量 `FLASK_PORT` 更改：`set FLASK_PORT=5001 && python app.py`；或直接双击 `start-dwg.bat`（固定 5001，与其他本地项目隔离）。
- 启动前有**端口占用预检**（`assert_port_free`）：端口被占即报错退出并给出排查命令，不再容忍 Windows 下同端口双进程静默绑定（Flask dev server SO_REUSEADDR 行为，曾致新旧两服务抢 5000、上传连接随机重置）。
- 排查命令：`netstat -ano | findstr :端口` → `tasklist | findstr PID` → `taskkill /F /PID PID`。

---

## 六、日志

运行日志写入项目根目录 `parser.log`（UTF-8），采用**按大小轮转**（`RotatingFileHandler`）：

| 项 | 值 |
| --- | --- |
| 单文件上限 | 10MB（`LOG_MAX_BYTES`） |
| 历史文件数 | 4 份（`parser.log.1` ~ `.4`，`LOG_BACKUP_COUNT`） |
| 磁盘占用上限 | ≈ 50MB |
| 启动预归档 | `_archive_oversized_log()`：启动时若 `parser.log` 已超阈值，先归档为 `.1` 并把历史序号整体后移，保证新一轮运行从干净文件开始；文件被其它进程占用时静默跳过（不阻断启动），由轮转机制在写入时自行切分 |

排查用法：`parser.log` 是当前日志，`.1` 是上一轮；每次解析的关键节点都输出日志（候选收集统计、INSERT 预筛、包裹框剔除、去重剔除、`全部候选 / 通过特征 / 去重后 / 上限后` 汇总、选中结果），可直接按文件名或 `全部候选` 检索定位。

---

## 项目文件说明

| 文件 | 作用 |
| --- | --- |
| `app.py` | Flask 后端服务：接收 DXF/DWG 上传，智能识别图框并返回宽×高（mm） |
| `index.html` | 前端上传页面 |
| `app.js` | 前端逻辑（调用 /upload 接口） |
| `style.css` | 前端样式 |
| `test_backend_rules.py` | 后端规则端到端测试（ezdxf 构造图纸，35 项全绿） |
| `test_judge_logic.js` | 前端尺寸分类逻辑单元测试 |
| `test_single_frame_tip.js` | 单图框空间来源气泡的数据构建 / HTML 生成 / 源码级回归测试（40 项） |
| `test_tip_interaction.js` | 气泡点击交互端到端 DOM 测试（jsdom，18 项；未装 jsdom 自动跳过） |
| `test_xlsx_export.js` | Excel 列宽自适应单元测试 + 真生成 xlsx 读回校验（33 项） |
| `test_xlsx_e2e.js` | Excel 导出端到端测试（jsdom 加载真实页面点击导出，24 项） |
| `test_layout_col_width.js` | 「布局分布」列宽专项测试（空/短/长内容对比 + 文件内实际列宽，7 项） |
| `test_delete_script_logic.js` | 删除脚本逻辑测试 |
| `vendor/xlsx-style.bundle.js` | xlsx-js-style 1.2.0（SheetJS 0.18.5 带样式分支，Excel 导出，本地引入不依赖 CDN） |
| `parser.log` | 运行日志（轮转，含 `.1`~`.4` 历史文件，已在 .gitignore） |
